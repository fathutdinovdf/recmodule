# Модуль управления рекомендациями — состояние работы

Краткая справка для нового чата. Что сделано, где лежит, какие решения приняты
и почему. Подробности — в комментариях к коду, они там объясняют «почему».

## Что это

Специализированный модуль КИС «Виртуальный расходомер» по договору
ЛУКОЙЛ-Западная Сибирь (Заказчик) ↔ АКЭ (Исполнитель): экспертное сопровождение
механизированного фонда, услуги с 01.07.2026 по 30.12.2027. Исполнитель выдаёт
рекомендации по скважинам, Заказчик решает, эффект подтверждается в 90-суточном
окне.

Было: статический HTML-макет (папка `макет/`) — девять экранов, выверенный
визуальный язык, документ решений. Стало: приложение на реальных данных
(папка `app/`). **Макет остаётся эталоном вёрстки**, из него переносятся
разметка и CSS один в один.

## Стек и запуск

Next.js 15 + React 19 + TypeScript, PostgreSQL 15.5 в Docker (та же версия, что
на дев-стенде ВМАП — чтобы не написать SQL, которого там нет).

```
cd app
npm run db:up      # поднять базу (нужен запущенный Docker Desktop)
npm run dev        # актуализирует демо-данные и поднимает сервер на :3100
```

Прочее: `npm run db:seed` — пересоздать демо, `npm run db:rebase` — пересчитать
базовые значения демо по замерам ВМАП (нужен доступ на стенд), `npm run
db:refresh` — сдвинуть даты к сегодня, `npm run db:load-economy` — перезалить
ставки.

## Устройство

```
app/
  db/migrations/     001_init (схема rec, 26 таблиц), 002_reference (справочники),
                     003_demo (якорь и сдвиг), 004_status_tone,
                     005_liquid_mass (жидкость в тоннах). Раннера нет,
                     применяются psql-ом вручную.
  scripts/           load-economy, seed-demo, refresh-demo, rebase-demo
  src/domain/        ЧИСТЫЙ TS без Next: measurements, effect, baseline, workhours
  src/db/            pool (две базы), vmap (только чтение), economy, recommendations
  src/services/      effect-window (замеры + ставки + база), effect-store (кэш
                     расчёта), baseline (база по замерам)
  src/components/    AppShell, Icons
  src/app/           страницы; registry.css и tokens.css — КОПИИ из макета
```

**Правило, заложенное в архитектуру:** предметная логика (расчёт эффекта,
агрегация замеров, рабочие часы) живёт в `src/domain/` без единого импорта из
Next. Причина: фронт ВМАП — React 18 SPA на Vite, не Next; при встраивании
серверная половина Next не переедет, а домен переживёт смену обвязки.

## Две базы

`modulePool` — своя, чтение и запись. `vmapPool` — дев-стенд Заказчика,
**только чтение**, в `vmapQuery` стоит проверка, отклоняющая всё кроме SELECT.

Дев-стенд: `vps-pgsql03.ois.ru:5432 / luk_cycleopvmap_dev / ois_vmap / ois_vmap`,
схема `ois_vmap`. ClickHouse `vps-chdb01.ois.ru:8123` — там телеметрия, но для
расчёта эффекта не используется.

## Источник данных: стенд или своя база (`DATA_SOURCE`)

На первом этапе модуль разворачивается на облачном сервере вне ландшафта
Заказчика, где стенда ВМАП нет вовсе. Поэтому всё, что бралось со стенда,
имеет вторую реализацию против своей базы, а выбор делает переменная
окружения:

| `DATA_SOURCE` | Откуда данные |
|---|---|
| не задана или `vmap` | дев-стенд ВМАП — как было, поведение по умолчанию |
| `manual` | своя база: `rec.ref_wells` и `rec.daily_facts` |

Переключение работает в обе стороны постоянно, это **не разовая миграция**:
при переносе на ландшафт Заказчика `DATA_SOURCE` убирается, и код ВМАП
возвращается в работу как есть. Выпиливать его нельзя.

## Демо-стенд в Yandex Cloud

Поднят 24.08.2026 для показа заказчику вне контура ВМАП, `DATA_SOURCE=manual`,
свои демо-данные (`db:seed`).

- VM `recmodule-demo`, Yandex Cloud, cloud `cloud-denisson2002`: 2 vCPU / 4 ГБ /
  20 ГБ SSD, Ubuntu 24.04, публичный IP `158.160.86.4` (динамический — при
  стопе/старте или пересоздании VM сменится, тогда поправить DNS и локальное
  исключение в VPN, см. ниже).
- Приложение — не в Docker: `next build` + `next start` под systemd
  (`recmodule.service`), сервисный пользователь `recmodule`,
  код в `/opt/recmodule`. Только Postgres в Docker
  (`app/docker-compose.yml`, порт 5433→5432 как локально).
- Домен `cycleop-rec.ru` (по аналогии с `cycleop-vmap`/`cycleop-design`/
  `cycleop-id`) — **работает по HTTPS**: `https://cycleop-rec.ru`. NS у
  reg.ru переведены на `ns1/ns2.yandexcloud.net`, зона и A-записи (`@`,
  `www`) — в Yandex Cloud DNS (`cycleop-rec-ru`, `yc dns zone ...`), не у
  reg.ru: их бесплатный DNS применяет правки часами (TXT-запись и смена
  A-записи один раз оба зависали на много часов без объяснимой причины —
  не пропагация в обычном смысле, а что-то на их бэкенде). Смена A-записи
  теперь через `yc dns zone update-records --name cycleop-rec-ru`.
- **Внешний IP демо-стенда переставал отвечать снаружи — чинится стопом/
  стартом VM.** 24.08.2026 вечером `130.193.55.90` перестал отвечать на TCP
  (22/80/3100) для ЛЮБОГО внешнего клиента (проверено с двух независимых
  сетей), при этом ICMP проходил, а изнутри Yandex Cloud (Cloud Shell,
  поддержка) всё отвечало штатно — сама ВМ и её ОС были полностью исправны
  (`ufw inactive`, `iptables INPUT ACCEPT`, сервисы слушают везде). Похоже на
  разрыв во внешнем анонсе/маршрутизации именно этого публичного IP после
  автоматической операции платформы (`Update instance` в логе, не наша).
  Обычная перезагрузка ОС (через серийную консоль) не помогла. Помог **стоп +
  старт самой VM через `yc compute instance stop/start`** — адрес сменился на
  `158.160.86.4`, и всё сразу заработало. Если повторится: `yc compute
  instance stop <id> && yc compute instance start <id>`, затем поправить DNS
  у reg.ru и исключение в VPN на новый IP.
- **HTTPS подключён.** Сертификат — бесплатный DomainSSL от reg.ru
  (GlobalSign DV), активен до 11.03.2027, файлы вручную подставлены в nginx
  (`/etc/nginx/ssl/cycleop-rec.ru.{fullchain.crt,key}`, ключ `chmod 600
  root:root`). `deploy/nginx.conf` переписан на два `server{}` — 80 редиректит
  на 443, 443 отдаёт TLS (`listen 443 ssl http2;` — важно именно так для
  nginx 1.24 на Ubuntu 24.04, новый синтаксис `http2 on;` эта версия не
  понимает). `COOKIE_INSECURE` в `.env.production` убран — больше не нужен,
  Secure-кука работает по-настоящему.
  Let's Encrypt через certbot не пошёл вообще: HTTP-01 — таймаут коннекта со
  стороны их серверов при том, что порт 80 снаружи открыт и отвечает; DNS-01
  — TXT-запись `_acme-challenge` у reg.ru не публиковалась часами. Оба —
  похоже на проблемы бэкенда reg.ru, не нашей стороны. `deploy/setup-vm-tls.sh
  <домен>` остаётся в репозитории как путь через Let's Encrypt на случай, если
  понадобится (например, для другого домена без таких тормозов DNS).
- Сценарий полного повторного разворачивания — `deploy/setup-vm-base.sh`
  (Docker, Node, клон приватного репозитория по deploy-key, БД, сборка,
  systemd) и `deploy/setup-vm-tls.sh <домен>` (nginx, certbot) — оба в
  `app/deploy/`. `.env.production` на VM не в гите (`deploy/.env.production.example`
  — шаблон).
- **`db:seed` не грузит справочник объектов и экономику — 25.08.2026 на этой
  VM их не было вовсе.** `db:seed` создаёт только пользователей и рекомендации;
  `rec.ref_wells` (орг-юниты: куст/месторождение/ЦДНГ, плотности) грузит
  отдельно `db:load-wells-ref`, а ставки экономики (`rec.econ_field_rates`/
  `econ_well_rates`/`econ_ndpi_rates`) — `db:load-economy`. Без них вкладка
  «Расчёт эффекта» показывала «Ставки не заведены» и «нет ни одного замера
  дебита» на всех карточках. `setup-vm-base.sh` теперь перечисляет эти шаги
  явно (между `db:migrate` и `db:seed`); при пересоздании демо-набора
  (`db:seed`/`db:refresh`) их заново гонять не нужно — справочник и ставки не
  привязаны к конкретным рекомендациям. А вот `db:seed-daily` (суточные данные
  ручного режима, замена `db:rebase` — тот ходит на стенд ВМАП, здесь
  недоступный) — привязан к датам демо-набора и нужен после каждого
  пересоздания.
- **Пересчёт эффекта в деньгах через интерфейс сейчас недоступен —
  архитектурное решение, не пробел.** Вкладка «Расчёт эффекта» только читает
  кэш `rec.effect_daily` (`services/effect-store.ts`); пересчитывает либо
  холодный старт (кэша нет вовсе — тогда первое открытие вкладки посчитает
  само), либо `POST /api/effect/recalc` — точка входа для бэкенда, кнопки в
  UI под неё нет. После разовой загрузки ставок 25.08.2026 кэш был пуст (его
  снёс `db:seed-daily`), поэтому пересчёт всех окон разом сделали вызовом
  этого эндпоинта по SSH, а не через экран.

**Устройство.** `src/db/wells-data.ts` — фасад, выбирающий реализацию;
все потребители (`services/baseline.ts`, `services/effect-window.ts`, мастер
регистрации, оболочка карточки) импортируют функции оттуда, а не из
`db/vmap.ts`. Сам `db/vmap.ts` при этом не менялся: у обеих реализаций одни
и те же имена и сигнатуры.

**Граница проведена по суточному ряду, а не по замерам.** Наружу оба источника
отдают `DailyPoint[]` (`dailySeriesFor`), а как ряд получен — их собственное
дело: стенд строит его протяжкой из замеров, ручной источник — прямо из
внесённых суток. Через `Measurement[]` значение приезжало искажённым (внесённые
100 → 100,00023): замер ставился на 00:00:01, и первую секунду суток занимало
прежнее значение. Суточная величина должна доходить до расчёта ровно такой,
какой её внесли.

**Последнее значение протягивается вперёд** — в обоих режимах одинаково.
Иначе один и тот же расчёт давал бы разные деньги в зависимости от
`DATA_SOURCE`, а незаполненный день читался бы как остановка скважины, которой
не было.

Протянутые сутки помечены `points = 0`, и это несущий признак, а не пометка:

- **отбор суток базы** (`domain/baseline.ts`) их НЕ берёт — договор требует
  кондиционных, то есть собственных значений;
- **расчёт окна** их берёт, но вкладка показывает «суток без своего значения,
  взято протянутое» и долю суток с собственным значением;
- **календарь** рисует их отдельным видом: сплошная — своё, кольцо —
  протянутое, пунктир — тянуть нечего (до первой внесённой записи).

Ограничение честное и общее с телеметрией: протяжка не отличает остановленную
скважину от той, по которой не внесли данные. Одни внесённые сутки в начале
окна дадут эффект за всё окно, и увидеть это можно только по доле собственных
значений. Лечится состоянием скважины, которого в расчёте пока нет.

**Справочник объектов** переносится дампом, в два шага, потому что доступ к
стенду и доступ к целевой базе не бывают одновременно:

```
npm run db:dump-wells-ref   # там, где виден стенд → вмап-скважины.json
npm run db:load-wells-ref   # на целевой машине → rec.ref_wells
```

Загрузка идёт UPSERT-ом, а не TRUNCATE: скважина, по которой уже выписаны
рекомендации, не должна на секунду исчезать из справочника посреди
загрузки, а пропавшие со стенда объекты остаются — на них ссылается история.

### Переезд на ландшафт Заказчика — граница по дате, а не подмена источника

Переключить `DATA_SOURCE` мало: сутки, посчитанные по внесённым руками
значениям, обязаны остаться такими, какими их посчитали. Иначе в день переезда
накопленный эффект по идущим окнам сдвинулся бы сам, без действия человека, —
а это деньги по договору.

Поэтому есть `rec.source_switch.vmap_from` (миграция 012): сутки **до** этой
даты берутся из `rec.daily_facts`, начиная с неё — из замеров ВМАП. Ряд
склеивается в `dailySeriesFor`; половины не пересекаются по датам, и обе уже
`DailyPoint[]`. Прошлое пересчитывается из тех же данных и даёт те же числа.

Почему по дате, а не «есть ручное значение — берём его»: ручные сутки хранятся
только там, где их вносили, промежутки заполняются протяжкой на лету. Смешение
по наличию записи дало бы чересполосицу источников внутри одного окна, и
объяснить происхождение цифры конкретного дня стало бы нечем. Одна дата — одна
формулировка в протоколе с Заказчиком.

Что при этом **не** переключается: плотности берутся у скважины и после
переезда приедут со стенда. Если там их обновили, прошлые сутки пересчитаются
с новым значением — перед переходом стоит сверить.

**Суточный факт** вводится во вкладке карточки «Суточные данные»
(`rec/[id]/daily`). Вкладка видна **всегда**, в том числе когда факт идёт из
ВМАП: внесённые сутки не исчезают при переезде, по ним посчитаны деньги, и
смотреть их — и править с журналом — нужно и там. Календарь раскрашивает
только сутки, которые действительно берутся из ручного ввода.

Ввод — календарь, а не таблица месяца. Вопрос, с которым туда приходят, —
«где в окне дырки»: сутки без собственного значения считаются по протяжке, и
чем их больше, тем меньше цифра опирается на данные. В сетке это видно формой,
в списке из девяноста строк — нет. Заливка кодирует происхождение значения:
сплошная — своё, кольцо — протянутое, пунктир — тянуть нечего. Подчёркивание —
сутки базы, точка в углу — что значение правили. Показывается отрезок от начала периода базы до
закрытия окна: ровно те сутки, что участвуют в расчёте этой рекомендации.
День правится в Popover прямо на клетке.

Пустое поле значит «своего значения нет» — сутки посчитаются по протянутому;
ноль значит остановленную скважину и даёт отрицательный прирост. Путать эти
два случая нельзя. Электроэнергия вводится там же, наравне с дебитом (как
в Форме 5), но **в деньги пока не идёт** — см. раздел о расчёте.

**Журнал правок** — `rec.daily_fact_events` (миграция 010), отдельная таблица,
а не пара столбцов «кто менял последним». На этих числах стоит расчёт эффекта,
то есть деньги по договору, и при разборе спора спрашивают не «кто трогал
последним», а как цифра дошла до нынешнего значения. Пишется каждое изменение:
старое и новое значение, автор и **номер карточки**, из которой правили.

Последнее важно из-за модели данных: факт принадлежит СКВАЖИНЕ, а не
рекомендации. Одни и те же сутки видят все рекомендации по этой скважине, чьи
периоды их накрывают, и правка из одной карточки меняет цифру для остальных.
Запись факта и запись события идут одной транзакцией, прежнее значение
читается под `FOR UPDATE`.

Заказчику вкладка доступна на чтение: поля заблокированы, кнопки нет, журнал
виден. Цифры, от которых считается его обязательство, он должен иметь
возможность проверить.

Экономика от источника не зависит: ставки НДПИ, ЭЭ, деэмульгатор и цена
нефти уже целиком локальные, `load-economy.mjs` берёт их из статических
дампов. Пласт при этом двух разных сортов: `rec.ref_wells.plast` (параметр 50
ВМАП) — информационное поле карточки, а пласт для НДПИ живёт в
`rec.econ_ndpi_rates` и приезжает из Excel-модели Заказчика.

## Реальный реестр на демо-стенде Yandex Cloud — переезд 26.08.2026

Демо-стенд `cycleop-rec.ru` больше не показывает демо-рекомендации. Заказчик
передал реальные данные — «Форма 2 Единый реестр мероприятий.xlsx» (116
рекомендаций, выгрузка 26.08.2026) и «Косынка ЛЗС.xlsx» (суточные факты по
90-суточным окнам эффекта + реальные ставки экономики, Лист2) — и попросил
дальше вести рекомендации на этом сервере вручную, через интерфейс, а не
демо-скриптами. **Локальный дев-ландшафт (докер на ПК) этот переезд не
затронул** — там по-прежнему демо-набор, воспроизводимый `db:seed`.

Как это устроено:

- **Разовый импортёр** — `scripts/import-real-registry.mjs`. Сухой прогон по
  умолчанию (только читает `rec.ref_wells`/`rec.users`), запись — только с
  `--apply`, снос демо (`is_demo`) — только с `--wipe-demo`. Источник —
  подготовленный заранее JSON (в репозитории не хранится, разбирался из
  исходных Excel вручную по колонкам; при повторном переносе данных придётся
  пересобрать таким же образом).
- **Ставки экономики** — `косынка-эффект.json` в корне проекта, тот же формат,
  что `альма-эффект.json`, только НДПИ/ЭЭ-нефть/деэмульгатор/цена нефти — из
  Лист2 «Косынки», а не из шаблона АЛЬМА от 05.08. НДПИ и ЭЭ-нефть совпали с
  прежними один в один, **ЭЭ на жидкость ниже** — в «Косынке» её явно считают
  за вычетом энергии на добычу нефти и газа, в старом шаблоне — нет. Это не
  ошибка, а более точная методика; `альма-эффект.json` не тронут — им
  по-прежнему пользуется дев (`db:load-economy` без `ECON_SOURCE`).
- **44 рекомендации + скважина 6102** заведены статусом «Согласовано», хотя в
  Форме 2 у них «Реализовано»/«Открыто окно»/«Частично реализовано»: ни в
  одном из двух файлов нет честной даты фактического внедрения (колонка
  «Дата открытия окна эффекта» в Форме 2 пустая у всех 116 строк без
  исключения), а без неё `rec.implementations` не завести. Настоящий текст
  статуса ALMA сохранён комментарием на карточке — дату донести и открыть
  окно вручную, когда она станет известна. Скважина 6102 — особый случай:
  статус в Форме 2 был «Реализовано», но «Косынка» прямо пишет «на
  согласовании» по столбцу H; окну доверили «Косынку».
- **9 рекомендаций** получили честное окно эффекта — дата и суточные факты
  (Qж/обводнённость/ЭЭ) взяты из «Косынки». Две скважины («227Л», «6102/6103»)
  имели по два кандидата в реестре — Косынкина запись отнесена к той, что
  ближе по дате передачи Заказчику к первой дате в косынке.
- **8 из 116** не привязались к `rec.ref_wells` (АГЗУ и узлы учёта — не
  скважины вовсе; плюс скважины 3010/куст 16 и 292/куст 69, которых нет в
  дампе `вмап-скважины.json`) — заведены с текстом из Формы 2 как есть, но
  без `well_id`/`field_id` (поля необязательны, см. 001_init.sql).
- **Направления** — в Форме 2 их 9, в договоре 6. Четыре лишних
  («КиП», «ЭПУ», «Сокращение потерь нефти за счёт стабилизации режима»,
  «Сокращение потерь нефти по скважинам ПКВ») добавлены в `rec.directions`
  миграцией `019_real_directions.sql` — таблица без enum именно на такой
  случай.
- **Новые пользователи Заказчика**: Ярашов Д.Р., Васин Н.А., Чернышов А.А.,
  Аржаников А.Е. (роль `engineer`, право решения) — их не было в демо-наборе,
  там только Гадаятов и Сафин.
- **Предохранитель `ALLOW_DEMO_RESET`** — `seed-demo.mjs`, `refresh-demo.mjs`,
  `rebase-demo.mjs`, `seed-daily-facts.mjs` отказывают запускаться, если в
  `.env.local` (голый `node` не читает `.env.local` сам — файл парсится
  вручную, как это уже делал `rebase-demo.mjs` для стенда ВМАП) нет строки
  `ALLOW_DEMO_RESET=1`. На деве строка есть, в `.env.production` на VM —
  сознательно нет: так демо-скрипты нельзя случайно запустить там, где
  рекомендации уже настоящие. Это единственная защита; сами файлы скриптов с
  сервера не удалялись и из репозитория никуда не делись.

## Расчёт эффекта — источники и формула

Формула из рабочего шаблона Заказчика «Шаблон оценки эффективности АЛЬМА»
от 05.08.2026 (лежит в корне проекта), применяется к ПРИРОСТУ к базе:

```
выручка   = Δ Qн × цена нефти
НДПИ      = Δ Qн × ставка по паре «месторождение + пласт»   ← с нефти, не с жидкости
ЭЭ жидк.  = Δ Qж(т) × ставка месторождения                 ← ЖИДКОСТЬ В ТОННАХ
ЭЭ нефть  = Δ Qн × ставка месторождения
реагенты  = Δ Qн × ставка месторождения
эффект    = выручка − НДПИ − ЭЭ жидк. − ЭЭ нефть − реагенты
```

**Электроэнергия в этой формуле — не замер, а удельная ставка.** Обе статьи
ЭЭ пропорциональны добыче (руб/т), то есть выражают энергозатраты на прирост
объёма. Изменение самого энергопотребления при той же добыче такая формула
выразить не может в принципе.

Форма 5 Заказчика считает иначе — энергию меряют и вычитают напрямую:

```
Эффект ЭЭ  = Факт ЭЭ − База ЭЭ            (кВт·ч/сут, минус — сокращение)
Эффект руб = Эффект нефти × цена нефти − Эффект ЭЭ × цена ЭЭ
```

Два документа Заказчика противоречат друг другу, и это не мелочь: направление
«Повышение энергетической эффективности» — одно из шести в справочнике, а по
формуле АЛЬМА эффект по нему **тождественно нулевой** (Δ Qж = 0, Δ Qн = 0 → все
статьи нули). Частично то же касается «Сопровождения периодического фонда».

**Что сделано пока:** база ЭЭ и суточный факт ЭЭ вводятся руками и хранятся
(`rec.daily_facts`, параметр 93; `baselines.base_ee` и `method_ee`), цена ЭЭ
заведена полем `econ_field_rates.ee_price`. В расчёт денег ЭЭ **не входит** —
формула не менялась. Складывать замеренную дельту с удельными статьями нельзя:
это двойной счёт одной и той же энергии. Правильный переход — замещение
(есть факт ЭЭ → статья по факту вместо двух удельных), но он меняет деньги по
договору и требует ответа Заказчика, какая из двух моделей верна.

**Жидкость в формулу входит массой.** В шаблоне столбец K — «Добыча жидкости,
т», ставка ЭЭ на жидкость (столбец N) умножается на него. ВМАП же меряет дебит
в м³/сут, поэтому прирост переводится в тонны: `liquidMass` в
`domain/measurements` — объём нефти из её массы и плотности, остальной объём
вода. Функция линейна, поэтому массу считаем прямо из приростов, и базе не
нужна собственная обводнённость. Плотности берутся у скважины: параметр 13
(нефть) и 12 (вода). Столбца Q «Дополнительные затраты» у нас нет: в шаблоне
это ручной ввод.

Прежний набор статей (подъём, ППД, транспорт, подготовка, обслуживание ГНО) — из
более ранней модели `01_2026_...xlsx`, Заказчик ей не пользуется, в модуле его
нет. Коэффициента эксплуатации нет намеренно: считаем по фактическим суткам, день
простоя приходит нулевым приростом сам, поправка задвоилась бы.

**Ставки:** 1210 скважин из 1707 фонда, 25 ставок НДПИ, 15 месторождений.
Сопоставление скважины со ставкой — по паре «месторождение + номер», НЕ по
номеру: номер уникален внутри месторождения, но не между ними, 77 скважин
получили бы ставку соседнего промысла молча.

**Базовые значения** вносит человек — на вкладке «Расчёт эффекта», действием
«Внести базовые значения». Автоматического расчёта при регистрации больше нет
и в мастере поля базы тоже нет: и то и другое противоречило договору.

Способов по договору (Приложение № 2, раздел «Формирование базовых значений»)
четыре, и они не равнозначны:

| Способ | Когда применим |
|---|---|
| `techregime` | **Основной.** Значения утверждённого технологического режима, действующего в месяце выдачи рекомендации. Только если с момента утверждения режима до регистрации изменений режима эксплуатации не было |
| `threeDays` | Средневзвешенные за трое суток до регистрации. Если такое изменение было либо режим не соответствует фактическим условиям. Договор помечает: «(по согласованию с Заказчиком)» |
| `agreedPeriod` | Иной репрезентативный период, когда кондиционных данных за трое суток не набралось. Только по взаимному письменному соглашению Сторон |
| — | Для ЭЭ способ **всегда отдельный**: `factual`, `threeDays` или `design`. Техрежим значения энергопотребления не содержит, взять её оттуда нельзя никогда |

Три вещи, которые из этого следуют и сделаны в форме:

1. **Условие основного способа фиксируется явно** — галочкой с перечнем
   изменений режима из пункта 3.3.1 договора. Без неё `techregime` не
   принимается: договор ставит способ под условие, а проверить его задним
   числом уже нельзя.
2. **Согласование Заказчика — поля, а не заметка** (`customer_agreed_at`,
   `agreement_ref`). Для `agreedPeriod` реквизиты обязательны: письменное
   соглашение по определению существует. Для `threeDays` их отсутствие не
   запрещает ввод, но остаётся видимым признаком в карточке.
3. **Хотя бы один файл-обоснование обязателен.** Каждый способ опирается на
   внешний документ, а на первом этапе, где значения берут руками, документ —
   единственное основание вообще. Файл привязан к версии базы
   (`attachments.baseline_id`), а не к рекомендации: версий бывает несколько.

**Границы по времени** ставит сервер, а не клиент: до открытия окна — обычное
внесение и замена; окно идёт — только исправление с обязательным основанием
(договор: «за исключением случаев выявления ошибок в исходных данных либо по
взаимному письменному согласованию Сторон»); окно закрыто — нельзя вовсе.
Фиксация реализации — **шлагбаум**: без базы она не проходит ни на экране, ни
на сервере, потому что открывает окно, после которого база уже не меняется.

`domain/baseline.ts` (расчёт по трём суткам) остаётся и используется справкой в
споре о базе: обе стороны видят договорную величину одинаково. Данные для
основного способа на стенде есть — параметры 0 «Дебит жидкости Режимный» и 6
«Обводненность объемная Режимная», 92 515 записей по 3190 скважинам, — но
автоматически он не считается: применимость решает человек.

**Суточные данные демо** ставит `npm run db:seed-daily`
(`scripts/seed-daily-facts.mjs`). В отличие от `db:rebase` он не ходит НИКУДА,
кроме своей базы, — в этом и смысл первого этапа. Значения строятся вокруг
базы карточки и ожидаемого прироста: выход на режим за трое суток, шум
несколько процентов, обводнённость выводится из пары «жидкость и нефть», чтобы
цифры на экране сходились между собой. Около 18 % суток окна оставляются
пустыми — иначе протяжка и счётчик собственных значений на демо не видны.

Строки помечены `is_demo`: сдвиг набора двигает их вместе с окнами, а
внесённое человеком не трогает. Сдвиг идёт **в два шага** (уехать на 100 000
суток вперёд и вернуться) — первичный ключ здесь `(well_id, parameter_id,
date)`, и уникальность PostgreSQL проверяет построчно, поэтому прямой сдвиг
падает на собственных строках.

**Демо-базы** ставит `npm run db:rebase` (`scripts/rebase-demo.mjs`) — по
настоящим замерам, тем же кодом домена. `seed-demo` пишет заглушку, потому что
работает без доступа к стенду. Запускать rebase после каждого seed и каждого
refresh: сдвиг дат демо переносит период базы на другие календарные сутки, а
замеры ВМАП настоящие и не сдвигаются.

**Замеры (факт):** параметр 1 «Дебит жидкости Замерной» + параметр 7
«Обводнённость» из `ois_vmap."WellData"`. Два места, где легко ошибиться:

1. **НЕ фильтровать по `DeleteDate IS NULL`.** DeleteDate у замера означает «на
   смену пришёл следующий», вся история — записи с проставленным DeleteDate:
   1 774 442 против 9 115 «живых».
2. **Время замера = `COALESCE(FactDate, LEAST(CreateDate, UpdateDate))`.**
   Семантика пары Create/Update в разных пачках разная; общее одно — момент
   замера всегда раньше момента записи в базу. FactDate точнее, но заполнен у
   сотни записей из девяти тысяч.

**Суточное значение** — интеграл по времени, а не среднее арифметическое
(замеры в 08:00 и 22:00 описывают куски суток разной длины). Между замерами
последнее значение протягивается: разрывы бывают до 50 суток. Известное
ограничение: остановленную скважину от скважины без замеров не отличаем —
для этого нужен параметр 16 «Состояние по ТМ».

## Демо-данные: якорь и сдвиг

Требование: на дев-версии всегда настоящие дата и время, но статусы демо не
устаревают.

Решение — в `rec.demo_state` хранится якорь (момент генерации набора).
`refresh-demo` считает разницу между якорем и сегодня, вызывает
`rec.shift_demo(days)` и переставляет якорь. Взаимное расположение событий
сохраняется полностью, меняется только привязка к календарю.

- Сдвиг, а не перегенерация — иначе терялось бы сделанное руками в интерфейсе.
- Записи без `is_demo` не двигаются: их даты настоящие.
- Сдвиг кратен суткам — иначе «утро понедельника» станет «ночью понедельника»
  и расчёт норматива в рабочих часах поедет.
- `effect_daily` при сдвиге удаляется, а не двигается: он привязан к реальным
  замерам ВМАП с их датами.

Набор: 65 рекомендаций по всем 10 статусам, 18 с окнами эффекта — все на
скважинах, по которым замеры реально есть. Есть решения, комментарии, базовые
значения, споры Заказчика (о дате реализации и о базе).

Статус «Закрыто окно» получен **досрочным закрытием**: окно в 90 суток,
открытое после 01.07.2026, к сегодняшнему дню само закрыться не успело.

## Сделано / не сделано

Готово: схема БД, экономика в базе, домен (агрегация + формула), оболочка
приложения, **реестр рекомендаций** — сверен с макетом по колонкам, ширинам,
иконкам, тегам и кружкам статусов, **сортировка, фильтры и период в шапке
реестра рабочие** (были декоративными). **Оболочка карточки** (`/rec/[id]`: шапка,
полоса ожидаемого результата, вкладки, правая колонка с параметрами скважины из
ВМАП), **вкладка «Расчёт эффекта»** со всеми блоками из решений ниже и
**вкладка «Сводка»** с первой записью из интерфейса в БД: решение Заказчика
транзакционно пишет `rec.decisions`, меняет статус и добавляет событие в
`rec.recommendation_events`. Формы учитывают роль и право принятия решения,
валидацию причины/обоснования и остаток SLA в рабочих часах. Расчёт
кэшируется в `rec.effect_daily`. Служебная страница расчёта убрана, её классы
из `app.css` удалены.

**Сортировка/фильтры/период реестра** перенесены из макета (логика была в
`app.js`: openFilterPopover/openTextPopover/openPeriodPopover, state.sort,
CTRL_ORDER) на URL-параметры и SQL вместо клиентского JS-состояния — отбор
считает сервер по всей таблице, а не браузер по одной странице, и ссылка на
отфильтрованный/отсортированный реестр рабочая сама по себе. Состав:
`db/recommendations.ts` — общий `WITH base AS (...)` CTE, где заодно посчитан
`control_kind` (бакет норматива ответа: hidden/pending/none/ok/late/overdue/
waiting) как SQL CASE, зеркальный `domain/workhours.control()` — колонка
«Контроль ответа» не лежит в БД и нигде раньше не сортировалась и не
фильтровалась, теперь можно; `columnFacet()` считает чек-лист значений со
счётчиком по всему реестру (не по текущей выборке — иначе счётчики одной
колонки менялись бы фильтром другой, как и в макете). Данные фильтра —
`GET /api/registry/facet?col=&q=` по требованию открытия поповера, а не
заранее: у «Скважины» может быть много разных значений. Клиентская половина —
`app/registry-head.tsx` (шапка `<thead>` как отдельный клиентский компонент,
колонки — общий `app/registry-columns.ts`, чтобы состав не расходился с
серверной страницей): три поповера (чек-лист, текстовый поиск, период) на
shadcn `Popover`, 3-тактная сортировка по клику на подпись колонки (включить →
сменить направление → выключить), как в макете. Анимация — animate-ui
`Highlight` (`components/animate-ui/primitives/effects/highlight.tsx`) даёт
скользящий фон у выбранного пункта периода, стрелка сортировки поворачивается
пружиной через `motion.svg` вместо CSS-transform. Для чек-листа Highlight не
взяли: его `children`-режим оборачивает строку в лишний `<div>`, ломая
flex-раскладку `.popover__row` (счётчик и подпись сходятся), а `parent`-режим
не нужен без общего выбранного значения — там осталась простая CSS
`:hover`-подсветка из `registry.css`, как в макете.

**Триггер поповера — `Hint` снаружи `PopoverTrigger asChild`, не наоборот**
(тот же порядок, что уже был в `card-actions-menu.tsx`): если `PopoverTrigger`
обёрнут вокруг `Hint`, Slot клонирует пропы (onClick и так далее) на сам
компонент `Hint`, а тот не прокидывает произвольные пропы дальше — клик молча
не открывал бы поповер.

**Не тронут `.popover` из `registry.css`** — у него `position: fixed`, а Radix
уже позиционирует `PopoverContent` сам через floating-ui; вместо класса
поповеру заданы те же отступы/ширина через Tailwind-утилиты с токенами
(`p-[var(--group-padding-s)]` и так далее), а `.popover__list/__row/__foot` и
`.field` — используются как есть, в них позиционирования нет.

Не реализовано (осталось как в макете, декоративно): перетаскивание ширины
колонок (`.resizer`) и «Настройка колонок» — выбор видимых колонок.

**Мастер регистрации открывается клиентским состоянием, не маршрутом.**
Раньше был перехватывающий маршрут `@modal/(.)rec/new` — URL менялся на
`/rec/new`, ссылку можно было скопировать. По требованию (открытое окно шарить
не нужно, черновик при перезагрузке сохранять не обязательно) заменено на
`registration-launcher.tsx`: кнопка держит `useState(open)`, справочники
(направления/приоритеты/исполнители/текущий пользователь) тянутся действием
`справочникиМастера()` из `rec/new/actions.ts` только в момент открытия —
адрес не меняется вообще. `RegistrationWizard` больше не дёргает
`router.back()`, принимает `onClose` пропом; для прямого захода по `/rec/new`
осталась обычная страница `rec/new/page.tsx` с обёрткой `standalone.tsx`
(`onClose` там — `router.push('/')`, состояния открытия у неё нет). Слот
`@modal` в `layout.tsx` и сам каталог `@modal/` убраны — перехватывающий
маршрут для этого сценария был не нужен. Черновик при закрытии/перезагрузке
не сохраняется (кроме отдельной ручной кнопки «Сохранить черновик» в самом
мастере — она была и раньше, работает через свой `localStorage`-ключ,
не автоматически); если понадобится автосохранение при F5 — обсуждали
localStorage поверх этого же launcher-а как отдельный шаг.

Начат поэлементный переход на shadcn поверх Tailwind 4 без изменения цветов
ВМАП. Уже используются Button, Calendar/Popover, Field, Dropdown Menu, Tooltip,
Combobox, Textarea, Item и Separator; Combobox сохраняет обычный контракт
HTML-формы и первым
применён к выбору причины отклонения рекомендации;
семантические цвета shadcn связаны с токенами ВМАП в `src/app/shadcn.css`,
Tailwind preflight выключен, чтобы не переопределять существующую вёрстку.
Меню действий показывает состав из `HEAD_ACTIONS`, но пункты намеренно disabled,
пока для них нет серверных операций. Tooltip покрывает кнопки-иконки в шапке,
реестре и карточке, а также сортировку, поиск, фильтры и период в таблице; у
стрелок календаря подсказок нет по решению пользователя.

**Вкладка «Реализация»** замкнула главную цепочку: регистрация → решение →
реализация → эффект. Исполнитель фиксирует дату, полноту и то, что не выполнено
при частичной; фиксация одной транзакцией пишет `rec.implementations`, переводит
статус в «Открыто окно», ставит `completeness` и включает уже готовый расчёт.
Там же разбирается спор о дате: Заказчик оспаривает дату, Исполнитель принимает
или отклоняет. Принятая дата переносит окно целиком и удаляет кэш `effect_daily`
— он посчитан по суткам старого окна. Фиксация несёт и вложения (`rec/[id]/impl/fix-form.tsx`)
— тот же `rec.attachments` с `content bytea`, что у реплик и обоснования рекомендации,
контекст `'implementation'`, лимиты те же (5 файлов, 10 МБ каждый). Список файла в форме — как
в композере обсуждения (`Attachment`-карточки), но заливка не XHR, а обычным полем формы:
скрытый `<input type=file>` синхронизируется с состоянием через `DataTransfer`, как в мастере
регистрации.

**Меню действий в шапке** (кебаб «⋯») работает целиком — `rec/[id]/lifecycle.ts`:
регистрация черновика с выдачей номера, удаление черновика, отмена, повторная
передача после уточнения, досрочное закрытие окна, новая рекомендация на основе
отклонённой. Пункт меню ведёт на вкладку с раскрытой формой, а не выполняет
действие сразу: операции необратимы, и между «нажал в меню» и «сделано» стоит
экран с последствиями. Меню видит только Исполнитель — все эти операции его.
Регистрация сама решает, уходит ли рекомендация Заказчику сразу: вне рабочего
окна она ждёт его открытия в статусе «Зарегистрировано».

**Спор о базе** замкнул вторую спорную величину — `rec/[id]/effect/actions.ts` и
`baseline-block.tsx`. Заказчик подаёт свою версию базы с обоснованием (новая
строка в `rec.baselines` со `status='proposed'`, `source='disputed'`, плюс спор
с `proposed_baseline_id`), Исполнитель принимает или отклоняет. Принятие делает
предложенную версию действующей, прежнюю — `superseded`, и удаляет кэш
`effect_daily`: каждые сутки в нём посчитаны как разность со старой базой.
Отклонение оставляет базу прежней, а предложенную версию — `rejected`, в
карточке она остаётся видимой. Формы — окном поверх карточки (ActionDialog), в
том числе подтверждение принятия: оно необратимо и стирает расчёт.

**Окна действий несут только заголовок, поля и кнопки.** Пояснения под
заголовком и строка фактов (номер, статус, остаток норматива) были и убраны по
решению пользователя: всё это стоит в самой карточке, из-под окна, и в окне
читалось повтором.

**Все формы действий теперь окном** (`ActionDialog`), а не блоком на вкладке:
решение Заказчика (принять / отклонить / уточнить), фиксация реализации, спор о
дате в обе стороны. Кнопки при этом остаются на месте — раньше форма занимала
их место, и после открытия было не видно, от чего отказываешься. Принятие даты
Заказчика получило собственное окно вместо мгновенной отправки: оно переносит
окно эффекта целиком и стирает `effect_daily`. Цена — формы требуют JavaScript;
размен тот же, что уже принят для меню действий и спора о базе.

**Три окна спора о базе открываются без навигации** — первыми из шестнадцати.
Окно стоит в разметке вкладки закрытым, открывает его состояние
(`ActionDialog` получил управляемый режим: `open`, `onOpenChange`, `trigger`),
а `?form=` задаёт лишь начальную открытость и убирается при закрытии руками.
Отказ валидации тоже перестал быть навигацией: действия возвращают ошибку
значением (`ОтветФормы` в `effect/actions.ts`), форма читает её через
`useActionState`. Раньше и открытие, и каждый отказ давали заглушку вкладки и
пересборку окна — это было видно глазом. Клиентская половина — в
`effect/baseline-forms.tsx`, серверная (что показывать и кому, справка по
замерам) осталась в `baseline-block.tsx`; справка обёрнута в `Suspense`, иначе
поход на стенд ВМАП задерживал бы вкладку ради текста внутри закрытого окна.
Цена: эти формы требуют JavaScript. Остальные тринадцать окон пока по-старому,
через адрес.

Заголовок, тон и ширина всех шестнадцати окон — в `rec/[id]/form-meta.ts`,
оттуда их берут сами окна: `<ActionDialog {...окно('reject')}>`. Файл без
`'use client'`: список читают и серверные страницы, и меню действий.

**Оставшиеся тринадцать окон переведены на тот же приём.** Открытость и
уборка `?form=` вынесены в общий хук `rec/[id]/use-dialog-state.ts`
(`useОкноДействия`), которым теперь пользуются все клиентские половины окон:
`effect/baseline-forms.tsx` (было), `summary/decision-forms.tsx` (решение
Заказчика — принять/отклонить/уточнить), `summary/lifecycle-forms.tsx`
(меню шапки — регистрация/удаление/отмена/повторная передача/копия) и
`impl/action-forms.tsx` (фиксация реализации, досрочное закрытие, спор о дате
в обе стороны). Все шесть серверных действий (`actions.ts`, `lifecycle.ts`,
`impl/actions.ts`) переписаны на тот же контракт `ОтветФормы`, что у спора о
базе: ошибка — значение из `useActionState`, а не редирект на `?err=…`.

Восемь окон с кнопкой прямо на вкладке (решение Заказчика, фиксация,
досрочное закрытие\*, спор о дате в обе стороны) получили `trigger` внутри
своего же компонента — открытие мгновенное и без навигации, один в один со
спором о базе. Пять окон из меню шапки (регистрация, удаление, отмена,
повторная передача, копия на основе) своей кнопки на вкладке не имеют:
меню — в `layout.tsx`, а окно — в дереве конкретной вкладки, разных
поддеревьях React, и без общего клиентского состояния между ними мгновенное
открытие недостижимо без более глубокой переделки (общий стор между шапкой и
вкладками). Поэтому для них `?form=…` по-прежнему единственный вход — как и
раньше, — но зато отказ валидации больше не редиректит и не мигает заглушкой:
окно уже смонтировано (гейт по статусу карточки, а не по URL) и просто читает
ответ действия. \*Досрочное закрытие окна эффекта тоже без кнопки на вкладке
— тот же случай, что меню.

Заодно ушёл `compl` из адреса вкладки «Реализация»: при отказе валидации формы
фиксации раньше полноту («полностью» / «частично») возвращали через параметр,
потому что редирект стирал состояние формы. Теперь форма не перерисовывается,
и выбор в переключателе остаётся как есть без всякой передачи через URL.

Пробовали открывать окно до ответа сервера — кнопка рисовала заглушку со
скелетоном, пока идёт навигация по `?form=`. **Откачено по решению
пользователя:** окна и так открываются быстро, а заглушка добавляла подмену
на стыке. Если возвращаться к скорости открытия, то правильный путь другой —
держать окна в разметке страницы закрытыми и открывать локальным состоянием,
оставив адрес вторым входом (перезагрузка, присланная ссылка, возврат ошибки
валидации). Тогда ждать нечего вовсе.

Скелетон остался там, где ожидание настоящее: `loading.tsx` у реестра, у всех
четырёх вкладок карточки и **у карточки целиком** — последний лежит в
`rec/loading.tsx`, а не в `rec/[id]/`: заглушка сегмента подставляется внутри
его собственного layout, а медленный тут именно `[id]/layout.tsx` (карточка,
соседи, история скважины, экономика, стенд ВМАП). Уровнем выше граница
накрывает и layout, поэтому каркас появляется сразу по нажатию на номер в
реестре. Полоса вкладок там тоже скелетон: счётчики и активная вкладка ещё
неизвестны, а настоящие подписи без подсветки читались бы как готовая полоса,
по которой почему-то нельзя нажать.

Ради этого же **оболочка переехала из страниц в корневой `layout.tsx`**: пока
`<AppShell>` стоял внутри реестра и внутри layout карточки, заглушка маршрута
подставлялась вместе с ней, и на время загрузки пропадали шапка с левой
навигацией. В корневом layout она вне всех границ Suspense — не
перемонтируется при навигации и сохраняет прокрутку. Цена: корневой layout
читает пользователя и стал динамическим (страницы и так `force-dynamic`). Каждая заглушка повторяет разметку своей страницы
(вплоть до ширин колонок реестра — таблица `table-layout: fixed` иначе прыгнет
по горизонтали), а не рисует общую сетку: смысл в том, чтобы появление данных
ничего не сдвигало.

**Вкладки карточки — сегментированный переключатель animate-ui** с едущей
подсветкой (`components/animate-ui/`, зависимость `motion`). Отход от макета,
где вкладки подчёркивались снизу. Панелей нет: содержимое вкладки — страница
маршрута, поэтому взята только полоса сегментов, каждый сегмент — `Link` через
`asChild`, `value` ведёт путь, `activationMode="manual"` (иначе стрелки
клавиатуры начали бы грузить страницы). Цвета переведены на токены: в
оригинале тема переключалась вариантом `dark:`, которого у нас нет.

## Шрифт — Roboto заменён на Inter (24.08.2026)

Руководителю не понравился шрифт макета ВМАП. Roboto в токенах и раньше не
грузился нигде (ни `<link>`, ни `next/font`) — по факту везде отдавался
системный `system-ui`, просто совпадавший с Roboto на Android. Заменили на
Inter по-настоящему, self-hosted через `next/font/google` в `layout.tsx`
(`subsets: ['latin', 'cyrillic']`, обязательно — интерфейс по-русски), переменная
`--font-inter` пробрасывается классом на `<html>`. `tokens.css`:
`--font-family-text` и `--font-family-headline` теперь `var(--font-inter),
system-ui, sans-serif` — правка в одном месте, `shadcn.css` (`--font-sans`) и
весь `registry.css` берут шрифт через эти токены и трогать не пришлось.
Golos Text (русский вариативный шрифт) рассматривался как альтернатива и
сравнивался side-by-side на артефакте — руководитель выбрал Inter.

## Экономическая модель — экран `/economy`, ветка `economy`

Первый экран раздела «Настройка». Отвечает не на вопрос «какие у нас ставки», а
на вопрос «посчитаются ли по ним деньги»: сверху готовность модели с разбором
пробелов, под ней таблица ставок. Пробел — это то, из-за чего расчёт эффекта
однажды встанет, и раньше его искали запросами к базе.

**Три расхождения с макетом, каждое сознательное.**

1. **Вкладок нет.** Макет разводил «Общие параметры», «Ставки по
   месторождениям» и «Ставки НДПИ» в три равноправные вкладки. Это не три
   равные сущности: одно число, 18 строк и 25 строк. Пластовая ставка —
   подробность месторождения, а не параллельный список: человек приходит с
   вопросом «что у меня по Южно-Ягунскому». Поэтому одна таблица объектов, а
   пласты раскрываются внутри строки (`Collapsible`, пружина по высоте).
2. **Просмотр и правка — разные режимы.** В макете каждая числовая ячейка
   всегда была полем ввода: экран из сотни полей читается как форма, а не как
   справка, и случайная правка четвёртого знака ничем не отличается от чтения.
   Вход в правку — отдельный жест, граница ответственности. Ускоритель —
   двойной клик по ячейке (входит в режим и ставит курсор туда же).
3. **Публикация — окно, а не выезжающая панель.** Панель делила бы правый край
   с историей, а публикация атомарна. В сводке показан относительный сдвиг:
   `12 480 → 14 200 (+13,8 %)` — скачок в разы виден до публикации, а не в акте
   через месяц.

**Атомарность.** Черновик живёт только в состоянии клиента; на сервер уходит
весь пакет разом. Применённая наполовину модель даёт сумму, которой не было ни
в одной редакции, и выглядит она правдоподобно. Старые значения читаются внутри
транзакции под `FOR UPDATE`, а не приходят с клиента: между открытием страницы
и публикацией мог опубликовать сосед, и журнал записал бы переход, которого не
происходило. Колонка выбирается по белому списку — имя поля приходит с клиента.
Пустой пакет версии не образует. Номер редакции — `ЭМ-ГГГГММДД-NNN`, порядковый
в пределах суток, считается по самой таблице версий в той же транзакции.

**Право.** `правитЭкономику` (`can_edit_economy`, решение 95) проверяется и на
экране, и повторно в серверном действии. Без права экран целиком в просмотре,
двойной клик не работает, вместо кнопки — плашка «Только просмотр» с указанием,
кто право выдаёт.

**Состояния.** Незаведённая ставка — прочерк, никогда не ноль: ноль утверждал бы,
что затрат нет, и молча занижал бы эффект. Месторождение без `source_name`
приглушено целиком, ячейки не взводятся даже в режиме правки. Очистка ставки в
сводке публикации помечена отдельно предупреждением: это перевод месторождения
в «не считается», а в общем списке правок она выглядит такой же будничной, как
смена числа.

**Найдено при проверке вживую и исправлено** (перечислено потому, что это
типовые грабли, а не разовые):

- `--item-gap-horizontal-xs` — это 2 px. Для пары «число + подпись» мало,
  слипается; нужен `-m` (8 px).
- Заголовки числовых колонок с единицей в одну строку («Деэмульгаторы, руб/т н»)
  шире колонки в 132 px и наезжают на соседний заголовок. Единица — второй
  строкой.
- Подпись справа от числа сдвигает число внутрь, и в одном столбце получаются
  два правых края — у строк с подписью и у всех прочих. Подпись ставится слева.
- Пружинный `CountingNumber` проезжает промежуточные значения, а склоняемое
  слово рядом считается по конечному: полсекунды висит «1 изменения».
  Анимированное число уместно только там, где за ним не идёт согласуемое слово.
- `.pagehead` в флекс-колонке сжимаем по умолчанию: подросший блок под ним
  выдавил заголовок экрана в нулевую высоту. Нужен `flex: 0 0 auto`.
- Элемент, выглядящий как чип-фильтр, но не нажимающийся, — ложная кнопка.
  Счётчик скважин без ставки стал фразой.

**Файлы.** `src/app/economy/{page,editor,actions}.tsx` + `economy.css`,
чтение — `econModel()` и `econHistory()` в `src/db/economy.ts`. Миграции не
потребовалось: схема `econ_*` уже соответствует решениям 91–95. Добавлен
`components/ui/sheet.tsx` из shadcn — панели истории в проекте не было.

**Склейка пластов с месторождением идёт по `source_name`, а не по
`field_name`:** ставки НДПИ пришли из модели Заказчика и названы её словами,
узел дерева ВМАП называется иначе, и четыре Южно-Ягунских узла ссылаются на одну
строку модели. Так её строит `scripts/load-economy.mjs`.

**Не сделано:** раздел «Справочники» (обсуждён, не начат) — см. ниже.

## Справочники — решено, но не реализовано

Обсуждено 24.08.2026, реализация впереди:

- Справочник «Рекомендуемые мероприятия» **убирается** — остаётся свободный
  текст.
- **История правок справочников добавляется** — общая таблица `rec.ref_changes`
  на все справочники (миграция `019`, номер свободен по всем неслитым веткам).
  Без версий и без обязательной причины, в отличие от экономики: правка
  одиночная и обратимая, требовать обоснование за переименование направления
  значит приучить писать «правка» в поле причины.
- **Право на правку справочников** — у роли `admin`.
- **Код месторождения** остаётся, но справочно, только на чтение; хардкод
  `КОДЫ` в `rec/[id]/lifecycle.ts` — источник истины, показывается из него же.
- **Причины уточнения** выводятся на экран пятой таблицей (`rec.clarify_reasons`
  уже есть с шестью значениями). Пары из решения 97 сдвигаются: на месте убранных
  «Рекомендуемых мероприятий» встают причины уточнения.

## Скорость загрузки

- **Клиентский кэш маршрутов** — `staleTimes: { dynamic: 180 }`. Был ноль, и
  возврат на вкладку, где человек был секунду назад, шёл на сервер заново.
  Серверный кэш по-прежнему выключен (`force-dynamic`), а действия сбрасывают
  кэш через `revalidatePath`. Три минуты выбраны под предзагрузку вкладок: с
  прежними тридцатью секундами она успевала протухнуть раньше, чем человек
  дочитывал сводку. Плата — страница, открытая повторно в пределах трёх минут,
  показывается такой, какой её прочитали: чужое решение или смена статуса на
  ней не видны.
- **Независимые запросы идут параллельно:** карточка и пользователь, расчёт и
  скважина. Было цепочкой.
- **Вкладки предзагружаются, но после открытой, а не вместе с ней.** `prefetch`
  у ссылок выключен: он запускается по появлению ссылки в поле зрения, то есть
  одновременно с загрузкой самой открытой вкладки — пять рендеров наперегонки
  за один сервер, — и не трогает вкладки, уехавшие за край горизонтальной
  прокрутки. Вместо него `ПрогревВкладок` в `tabs.tsx`: `useEffect` (то есть
  после гидрации открытой вкладки) → пауза до простоя → остальные вкладки по
  очереди, а не залпом. Прогретое протухает вместе с кэшем роутера, поэтому
  прогрев повторяется каждые 150 секунд (`staleTimes.dynamic` = 180, обновляем
  с запасом) — иначе после минуты чтения сводки переход снова шёл бы на сервер со
  скелетоном. Активная вкладка читается из ref, чтобы переключение не
  перезапускало цикл; в скрытой вкладке браузера тик пропускается, при возврате
  прогрев идёт сразу. На деве повтора нет: там прогрев ничего не кэширует, а
  только компилирует маршрут, и второй раз это уже ничего не даёт.
  **В деве prefetch у Next отключён наглухо** — `createPrefetchURL` возвращает
  null при `NODE_ENV === 'development'`, и `router.prefetch` молча ничего не
  делает, как и `Link`. Поэтому там маршрут прогревается своим `fetch` с
  заголовком `RSC: 1`: в клиентский кэш роутера такой ответ не ляжет (это умеет
  только роутер), но маршрут скомпилируется и данные прочитаются — на деве
  задержку составляет именно это.
- **Страница эффект НЕ считает — решение пользователя, архитектурное.** Читает
  готовое из `rec.effect_daily`. Пересчёт — дело бэкенда, который в рабочем
  контуре поднят постоянно; походы на чужой стенд не должны зависеть от того,
  открыл ли кто-то вкладку. Точка входа — `POST /api/effect/recalc`
  (`recalcEffect` в `services/effect-store`), пересчитывает все открытые окна;
  на демо-наборе это 12 окон за 6 секунд. Раньше те же 90 суток тянулись со
  стенда при каждом открытии вкладки, хотя замеры приезжают раз в сутки.
  Единственное, что осталось в рендере, — холодный старт: если сохранённого
  расчёта нет ни одной строки, страница считает один раз. С появлением
  постоянного бэкенда это исключение убирается.
- **На демо роль планировщика играет подъём сервера.** `npm run dev` идёт через
  `scripts/dev.mjs`: он поднимает Next, дожидается ответа и дёргает
  `POST /api/effect/recalc`. Пересчёт фоновый, старт не задерживает.
  **Хук `instrumentation.ts` для этого не годится** — проверено и откачено:
  Next собирает его для всех слоёв, включая клиентский, а туда через `pg`
  тянется `fs`, которого там нет; модуль не собирается, и вместе с ним не
  собирается приложение — все страницы отдают 500. Заодно `pg` вынесен в
  `serverExternalPackages`, и предупреждения про `pg-native` исчезли.

Три решения, принятых по ходу, — в комментариях к `effect/actions.ts`:

- **Границы спора:** сверху — закрытие окна эффекта (по аналогии со спором о
  дате; в договоре про базу прямо не сказано), снизу — согласование
  рекомендации. До согласования отдельный спор не нужен: не согласен с базой —
  не согласовывай или запроси уточнение.
- **База по замерам показывается обеим сторонам одинаково** — и Заказчику при
  подаче, и Исполнителю при разборе, с пометкой «не позиция ни одной из
  сторон». Асимметрия превратила бы справку в подсказку одной стороне.
- **Повторный круг разрешён:** после разбора Заказчик может подать возражение
  снова, пока окно не закрыто. Мешает только незакрытый спор.

Прогнано вживую на демо-базе: обе стороны, все шесть операций меню, фиксация
(полная и частичная), спор о дате в обе стороны, спор о базе в обе стороны,
отказы валидации, попытка Заказчика разобрать собственное возражение. Снимки —
`скриншоты/реализация/`, `скриншоты/действия/`, `скриншоты/спор-о-базе/`.

**Вкладка «История и обсуждение» — чат в реальном времени.** Одна лента на
события процесса и реплики сторон: спорная ситуация разбирается по одной
хронологии, две отдельные вкладки заставляли бы сшивать их глазами. Файлы —
`app/src/app/rec/[id]/log/` (`chat.tsx` — состояние, SSE, прокрутка,
оптимистичная отправка; `composer.tsx` — ввод, упоминания, вложения;
`rows.tsx` — строки; `format.ts` — форматирование, общее для сервера и
клиента), чтение — `src/db/log.ts`, канал — `src/db/notify.ts` и
`api/rec/[id]/stream`, приём реплики — `api/rec/[id]/comment`, отдача файла —
`api/attachment/[id]`. Миграция `006_chat.sql`.

Принятые решения:

- **SSE, а не WebSocket.** Поток нужен в одну сторону, отправка идёт обычным
  POST. SSE работает штатным маршрутом Next и переезжает в SPA ВМАП как есть,
  WebSocket потребовал бы второго сервера. Оповещение между процессами — через
  PostgreSQL LISTEN/NOTIFY (триггер на `rec.comments`), а не общую память:
  писать реплику может любой процесс.
- **Соединение живёт 5 минут**, потом закрывается штатно, браузер
  переподключается сам. Вечный поток мешает dev-серверу пересобираться.
- **Вложения лежат в базе** (`bytea` в `rec.attachments`): тома у модуля нет, а
  при встраивании не будет и своего диска. Предел — 10 МБ на файл, до пяти за
  раз; `storage_key` оставлен под будущий переезд в S3.
- **Упоминания хранятся связью** (`rec.comment_mentions`), а не текстом:
  фамилия в тексте завтра поменяется, связь останется.
- **Реплика не двигает процесс.** Событие в `recommendation_events` она не
  пишет: уточнение по-прежнему запрашивается решением, а не сообщением.
- **Прогрессивной деградации нет:** без JavaScript реплику не отправить.
  Осознанный размен ради живого чата, расходится с прежним правилом «формы
  работают без JS».
- Фильтров ленты нет (были — убраны), поле ввода липнет к низу панели,
  прокрутка — общая, у панели вкладки.
- **Счётчик у вкладки живой, и его слушает не чат, а полоса вкладок.** Она
  лежит в layout карточки и переживает переходы, поэтому реплика, пришедшая
  пока человек смотрит расчёт эффекта, видна сразу: число растёт и появляется
  акцентная точка (`tab__n--new`, `tab__dot` в `card-extra.css`) — одного
  изменившегося числа мало, прежнего человек не помнит. Пока открыто само
  обсуждение, полоса канал не слушает: у чата свой, а два соединения к одному
  потоку — лишний расход и в браузере, и на сервере.
- **Вкладка обсуждения при открытии просит `router.refresh()`.** Клиентский
  кэш живёт три минуты, а догрузки пропущенного у чата нет: история приходит
  серверным рендером, дальше только канал, и свежее подключение принесёт лишь
  то, что появится после него. Свежий рендер сливается с состоянием по ключу
  (замена стёрла бы неотправленные реплики).

Список упоминаний в чате — тот же cmdk в оболочке `combo__*`, что у остальных
выпадающих полей карточки, с зеркальным въездом `comboInUp`: меню раскрывается
вверх, значит и двигаться должно снизу. Поле ввода — `Textarea`, подсказки
кнопок — `Hint`, пилюля «Новое сообщение» и «Повторить» — `Button`.

**Вкладка «Файлы»** — единая витрина `rec.attachments`: показывает все
вложения рекомендации независимо от того, приложены они к обоснованию,
решению, реализации или реплике. У файла остаются контекст, автор и дата; для
реплики виден фрагмент текста, поэтому выгрузка не отрывается от разговора, в
котором появилась. Вкладка сама файлы не загружает намеренно: материал должен
появляться в том действии или реплике, смысл которых подтверждает. Скачивание
идёт через `api/attachment/[id]`, который проверяет пользователя и всегда
отдаёт файл вложением, а не открывает потенциально опасный HTML на домене
модуля. Число файлов показано в полосе вкладок.

**Переключатель темы** — `components/ThemeToggle.tsx`, иконка в шапке. Тема
хранится в `localStorage` рабочего места (не кука и не база: серверу о ней знать
нечего), до первой отрисовки её ставит синхронный скрипт в `layout.tsx` — иначе
тёмная страница мигает светлым. `prefers-color-scheme` намеренно не слушаем.
Заодно исправлены три текстовых токена тёмной темы (решение 99, пересматривает
68): `--text-primary`, `--text-tertiary`, `--text-quaternary` переопределены в
`shadcn.css` — в выгрузке Figma они тёмные, предназначены белой подложке
`--bg-component-base`, а её в модуле нет ни в одном правиле. Правка не в
`tokens.css`: тот файл — дословная копия выгрузки.

Прокрутка страниц: системная полоса скрыта, вместо неё накладной индикатор
`components/ScrollOverlay.tsx` — появляется при прокрутке, гаснет, места не
занимает, поэтому вёрстка не сдвигается при смене длины содержимого.

Сделано с прошлой редакции плана: мастер регистрации (`/rec/new`), пользователи
и роли (`/users`, ветка `auth`), вход по паролю (`/login`), заявки о проблемах
(`/problems`, сверх исходных девяти экранов макета).

Дальше по плану, в порядке ценности — статус каждого экрана и подробности в
`feature_list.json`:

1. Справочники (администрирование — сами данные уже используются в мастере),
   заявки Заказчика, календарь и SLA — три экрана макета, ещё не перенесённые.
2. Экраны «Окна эффекта» и «Подтверждённые», Акт верификации (Приложение № 5).

## Инбокс «Мои задачи» — перенесён 28.08.2026 (ветка `inbox-app`)

`/inbox`: экран собран **только для эксперта Исполнителя** — решение
пользователя на этот заход. Билдеры ролей — карта `БИЛДЕРЫ` в
`app/src/app/inbox/page.tsx`: добавление expertLead / engineer / customerLead —
добавление функции, не переделка. Роли без билдера видят заглушку «Инбокс для
вашей роли ещё не собран» со ссылкой в реестр; администратор — свою, со ссылкой
на «Пользователи и роли» (решение 82).

Шесть блоков эксперта перенесены из `макет/inbox.js` со всей предметной
логикой: уточнения и споры о дате (hot), черновики (warn), «ждут передачи» /
«согласовано» / «окна закрываются» (наблюдение); плитки-якоря сверху, лимит
6 строк в блоке, тексты «why» — из макета. Данные — `app/src/db/inbox.ts`:
один запрос поверх экспортированной из `recommendations.ts` `основа()`
(граница видимости + готовый `control_kind` — контроль ответа НЕ
пересчитывается вторым источником), спор о дате — LATERAL, горизонт
«закрывается скоро» — `параметрМодуля('closingSoonDays')` из
`rec.module_params` (решение 81).

Решения по ходу: deep-link окон ведёт на `/?tile=window` (отбора
`alert=window` у реестра нет); момент передачи для `registered` без `sent_at`
считается как в колонке «Контроль ответа» (`toWindow(registeredAt)`); плитка
называется «Ждут передачи», а не «Уйдут в 09:00» — момент передачи не всегда
09:00.

**Значок в левой навигации** — живой: `счётчикИнбокса(role)` в `db/inbox.ts`
(отдельный COUNT, не полные строки — значок стоит на каждой странице), читает
`AppShell`, рисует `AppChrome` вместо прежнего захардкоженного «7». В значок
входят только блоки «ход за мной» (уточнения + споры о дате + черновики) —
наблюдательные не входят, иначе значок показывал бы объём ведения, а не число
дел на сегодня. Ноль не показывается. У ролей без билдера и админа значка нет
(`РОЛИ_С_ИНБОКСОМ` в `db/inbox.ts` — расширять вместе с `БИЛДЕРЫ`).

Замечено при переносе, не решено: `границаВидимости` для эксперта — это
`executor_id = me OR author_id = me`, то есть черновик чужого автора, где я
исполнитель, в реестре и инбоксе виден, что строго расходится с правилом
«черновик видит только автор». Инбокс сознательно унаследовал поведение
реестра, чтобы числа сходились; если правило строгое — править
`границаВидимости` в `lib/access.ts` отдельным решением.

## QA-прогон 28.08.2026 (агент, только отчёт)

75/75 юнит, 18/18 интеграционных, tsc чист. Все четыре правки от 28.08
(колонки, ИИ-резюме, кружки ленты, UI-фиксы) проверены вживую в обеих темах,
таблица тонов `log/rows.tsx` сверена с `rec.statuses` — совпадает вся.
Исправлено по итогам: **Яндекс.Метрика получила гейт `NODE_ENV ===
'production'`** — раньше дев-запуски слали хиты и вебвизор в боевой счётчик, а
при недоступном `mc.yandex.ru` консоль каждой страницы сыпала
`ERR_CONNECTION_RESET`. Не исправлено, отмечено: у ИИ-резюме нет таймаута в UI
(у SDK умолчание 10 минут); сортировка и фасет «Куст» лексикографические
(`0, 139, 140, 16, …` — поле текстовое, значения почти все числовые).

## Проверка вживую

`node scripts/render.mjs /rec/148/impl C:\путь\снимок.png` — снимок страницы
headless-браузером при поднятом dev-сервере. Роль на снимке задаёт переменная
`DEMO_USER` при запуске сервера (`DEMO_USER=matrosov npm run dev:only`): у
браузера в свежем профиле кука нет, а половина интерфейса зависит от стороны.

Два места, где легко потерять час:
- **Chrome нужен свой профиль** — с профилем по умолчанию запуск при открытом
  браузере пытается достучаться до работающего экземпляра и висит навсегда;
- **новый файл в проекте — снести `.next`**: dev-сервер отдаёт
  `__webpack_modules__[moduleId] is not a function` на страницу, которая
  компилировалась до появления модуля.

Действия проверяются отправкой формы из консоли браузера
(`document.querySelector('.form form').requestSubmit()`) и запросом к базе
`docker exec recmodule-db psql -U recmodule -d recmodule`.

## Ручной источник данных — слит в master 22.08.2026

Ветка `data-source-manual` (14 коммитов) слита в master 22.08.2026, worktree
`../wt/data-source-manual` больше не ведущий. Слияние было трёхсторонним, с
конфликтами в трёх файлах; как их разобрали:

- **`ui/Popover.tsx`** — вставки обеих сторон пришлись на одну строку, но не
  пересекаются: `PopoverAnchor` (нужен `Combobox`) и заголовочная тройка
  (нужна календарю суток) оставлены обе.
- **`rec/[id]/effect/page.tsx`** — графики от master (recharts), тексты
  «Качества данных» от ветки (ветвление по `РУЧНОЙ_ИСТОЧНИК`). Примечание под
  плитками master убирал вместе с прочими серыми простынями страницы, но здесь
  оно возвращено: только там объясняются протяжка, разница нуля и пустого поля
  и сообщение о сохранённом расчёте по закрытому окну.
- **`rec/new/wizard.tsx`** — победила ветка: базы в мастере нет вовсе, а не
  «временно отключена», как её оставлял master. Заодно ушли поля `baselineSource`,
  `baseQzh`, `baseQn`, `baseEe`, `baselineNote` из черновика, закомментированный
  fetch превью базы и `api/registration/baseline`. Из master сохранены
  `draftButton`, условие `черновикВБазу` и тексты окна закрытия — условие
  упрощено, полей базы в нём больше нет.

Строка «разрыв линии — суток без данных и без чего протянуть» вернулась в
легенду `fact-chart.tsx`: при переносе легенды внутрь компонента она потерялась.

**Что доделать до переезда к Заказчику:**

1. **Прогнать режим `DATA_SOURCE=vmap` вживую** — он ни разу не запускался,
   проверялся только ручной. Типы сходятся, но это не проверка.
2. ~~Починить `npm run db:migrate`~~ — сделано в master: раннер
   `scripts/migrate.mjs` с журналом `rec.schema_migrations`. Номера ветки
   (009–014, 016) с master (008) и `agent/auth` (015) не столкнулись.
3. **Закрыть вопросы 2.7 и 2.8** по электроэнергии: какая модель верна и
   откуда брать цену руб/кВт·ч.
4. **Снять дамп справочника с ПРОДУКТИВНОГО ландшафта ВМАП**, а не с дев-стенда
   (`db:dump-wells-ref` → `db:load-wells-ref`). Плотности оттуда пересчитают
   прошлые сутки — это принято сознательно.
5. **Проставить `rec.source_switch.vmap_from`** в день переезда, иначе граница
   не сработает и открытые окна пересчитаются из телеметрии целиком.

**Чего сознательно нет:** действия «Согласовать базу» у Заказчика — согласование
записывает Исполнитель как факт с реквизитами; массовой загрузки суточных
данных файлом; признака кондиционности суток отдельным полем (пустое значение
и есть «некондиционные», см. решение 105).

## Роли (`auth`) слиты в master 22.08.2026, плюс заявки о проблемах

Ветка `auth` (роли, вход по паролю, серверные сессии — `rec.roles`,
`role_key` у пользователя, `rec.sessions`, `/login`, экран «Пользователи и
роли») слита в master. Один конфликт — в `ui/Select.tsx`: обе ветки
независимо добавили `value`/`onValueChange` (контролируемый режим), разрешён
объединением с `triggerClassName` из master. Роль `admin` появилась впервые
именно в этой ветке — до слияния «администратора» не существовало вовсе,
только `side`/`canDecide`/`canEditEconomy`.

Ради роли `admin` и была слита `auth`: кнопка «Сообщить о проблеме» в подвале
навигации (стояла без действия) теперь видна Исполнителю и администратору
(`этоИсполнитель(user) || этоАдминистратор(user)`, обе — в `lib/access.ts`) и
открывает диалог с текстом и опциональными скриншотами (до 3, до 10 МБ,
паттерн один в один с композером обсуждения — `rec/[id]/log/composer.tsx` и
мастером регистрации). Заявка пишется в `rec.problem_reports`
(миграция `017_problem_reports.sql`), файлы — в `rec.attachments` через новую
колонку `problem_report_id` (та же таблица, что и вложения рекомендаций и
реплик, `rec_id` при этом NULL). Список для администратора — `/problems`,
лента карточек по образцу вкладки «Файлы» (`rec/[id]/files/page.tsx`), а не
таблица: текст заявки произвольной длины плохо ужимается в ячейку.

**Письмо о заявке подключено условно** — `lib/mail.ts` (nodemailer),
вызывается из `problem-report-actions.ts` после записи в БД, в try/catch:
сбой почты не портит форму. Пока `SMTP_HOST` в `.env.local` пуст, письмо
просто не уходит — заявка всё равно сохранена. Заведён на Yandex SMTP
(`smtp.yandex.ru:465`, пароль приложения, не пароль от аккаунта); значения
`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_FROM`/`PROBLEM_REPORT_EMAIL` вписываются
в `.env.local` вручную и в гит не идут.

**Отметки «решено»/«просмотрено» у заявки нет сознательно** — попросили только
увидеть список, заводить статус-модель без этого запроса значило бы
придумывать за администратора рабочий процесс.

**Про kibo-ui**: изучил компоненты реестра (`List` — кanban с drag-and-drop,
не подошёл; `Table` — TanStack Table+Jotai, тяжело для одной read-only ленты
с текстом переменной длины; `Status`/`Pill`/`Relative Time` — не тот смысл).
Ничего не встал в масть лучше, чем уже готовое в проекте — переиспользован
свой `Attachment` (де-факто тот же паттерн, что и kibo-ui Dropzone/attachment,
уже стоит в `components/ui/attachment.tsx`), `ActionDialog`, `Textarea`,
`SubmitButton`. Ни один kibo-ui компонент в итоге не добавлен.

## Уведомления и колокольчик — заведены 22.08.2026

Три части, продуманы и реализованы вместе: персистентный бейдж «непрочитано»
на вкладке «История и обсуждение», уведомление по факту @-упоминания
(упоминание в тексте и его хранение в `rec.comment_mentions` уже были — не
хватало именно уведомления) и глобальная панель по колокольчику в шапке.

**Схема** — `rec.notifications` (миграция `018_notifications.sql`): одна
строка на получателя, `type IN (mention, comment, status_change)`,
`read_at NULL` = непрочитано. Раньше «непрочитано» на вкладке обсуждения было
только состоянием открытой сессии (SSE-счётчик в `tabs.tsx`, обнулялся при
заходе на вкладку, ничего не хранилось в БД) — по явному решению пользователя
сделано персистентно, переживает перелогин и разные устройства.

**Получатели** (правило от пользователя, не выведено самостоятельно):
Исполнитель — все эксперты и руководитель экспертов (`role_key IN ('expert',
'expertLead')`) — в курсе **всегда**, независимо от участия; Заказчик
подключается только там, где уже участвовал перепиской (`EXISTS` по
`rec.comments.author_id`). Одинаково для `comment` и `status_change`. Для
`mention` получатель — только упомянутый, без разбора участия.

**comment vs mention — не триггер, а `db/notifications.ts`**
(`insertCommentNotifications`, вызывается из `api/rec/[id]/comment/route.ts`
в общей транзакции с самой репликой). Причина: на момент вставки комментария
`rec.comment_mentions` ещё пуста — она заполняется отдельным запросом сразу
следом, — а разводить получателей `comment`/`mention` нужно одним взглядом на
оба списка сразу, без гадания об очерёдности триггеров. Упомянутый получает
`mention`, а не вдобавок ещё и `comment` — второе было бы дублем одной и той
же реплики.

**status_change — наоборот, триггером** (`rec.notify_status_change()` на
`AFTER INSERT ON rec.recommendation_events`, тот же приём, что у
`comment_notify` из миграции 006). Событие статуса пишут четыре разных файла
(`lifecycle.ts`, `actions.ts`, `impl/actions.ts`, `effect/actions.ts`) через
общую по смыслу, но не переиспользуемую впрямую функцию `событие()` — обрастать
вызовом «уведомить получателей» в каждом означало бы один раз забыть. Триггер
проверен вручную (`INSERT ... ROLLBACK` в транзакции) — получатели верные.

**Бейдж вкладки** (`tabs.tsx`, `ЖивойСчётчикОбсуждения`) теперь стартует не с
нуля: персистентный остаток приходит пропом из `layout.tsx`
(`getUnreadCountForRec`) и инициализирует точку «есть новое» ещё до первого
SSE-события. Открытие самой вкладки (`log/page.tsx`) гасит непрочитанное по
рекомендации — `markRecRead` вызывается прямо в серверном рендере страницы,
это и есть момент «увидел».

**Колокольчик** (`components/NotificationBell.tsx`) — Popover на кнопке,
которая раньше была декоративной (`AppChrome.tsx`, `<span className="dot" />`
без обработчика). Список читает `AppShell.tsx` (`listNotifications`, 30
записей) и передаёт пропом — тот же путь, что уже был у `user`/`users`; после
`отметитьУведомление`/`отметитьВсеУведомления` (`lib/notification-actions.ts`,
`'use server'`) `revalidatePath('/', 'layout')` перерисовывает именно этот
компонент, как и у `switchUser`. Список один на всех — не по ролям: разница в
том, кому какие строки попали в `rec.notifications`, а не в устройстве самой
панели. Хронология без пересортировки по «прочитано» — только заливка и точка.

**Грабля, на которую наступил и здесь**: `<PopoverTrigger asChild><Hint>...`
не работает — `Hint` не форвардит пропы/`ref` дальше (не `forwardRef`,
`{children, text}` без spread), и Radix теряет обработчики открытия. Рабочий
порядок — `Hint` снаружи, `*Trigger asChild` внутри него
(`<Hint><PopoverTrigger asChild><button>`), как уже было сделано в
`card-actions-menu.tsx` до этой правки — просто не был замечен как общий
паттерн, пока не собрал по своей интуиции в обратном порядке и не словил
немую кнопку. Проверено CDP-скриптом (клик по реальным координатам, не
`.click()`) и снимком `npm run shot` — попап открывается, бейдж считает.

**Не сделано, вынесено в открытые вопросы ниже**: внешний канал уведомлений
(LUKTEAM/почта, вопрос 3.5) — колокольчик зафиксирован как единственный
in-app-канал на сейчас. Отдельная страница «Все уведомления» вместо только
попапа — не заводил, 30 записей в попапе хватает на первую итерацию.

## Открытые вопросы — спросить Заказчика или Эльдара

1. **Период базы: 3 суток или 7.** Реализованы 3 — по договору. Семи суток в
   договоре нет ни в одном из четырёх способов, «иной расчётный период»
   применяется только по письменному соглашению Сторон. Меняется константой
   `BASELINE_DAYS`, но тогда в карточке нужна отметка о согласовании.
2. **Кондиционность суток** определяем беднее договора: «соответствие
   фактическому режиму эксплуатации» без состояния скважины не проверить.
   Данные для этого есть — параметр 78, 3554 скважины.
3. **База ЭЭ** вводится руками и в расчёт не входит: источника факта нет.
   На стенде есть параметры 91 «Активная электроэнергия за предыдущие сутки,
   кВт·ч», 93 «Фактическое потребление», 94 «Расчётное потребление» — договор
   при отсутствии прибора учёта предписывает четвёртый способ, через Design.
4. **Прогнозный эффект в рублях выводится, а не хранится:** эксперт вводит
   Δ Qж и Δ Qн, деньги считаются из них той же формулой. Отдельное поле рублей
   завело бы второй источник правды. Если Заказчик хочет вводить рубли —
   нужна колонка и решение, что делать при расхождении с приростами.
5. **Столбец Q «Дополнительные затраты»** в шаблоне АЛЬМА есть (ручной ввод по
   мероприятию), у нас статьи нет. Подставлять ноль молча не стали.
6. **Первичность рекомендации** (поле макета `isPrimary`) в схеме отсутствует;
   в шапке карточки на этом месте показано решение Заказчика.
7. **До какого момента можно оспаривать базу — в договоре не сказано.** Сделано
   по аналогии со спором о дате: пока не закрыто окно эффекта, и не раньше
   согласования рекомендации. Верхняя граница спорна: у базы, в отличие от даты,
   есть довод и за более раннюю границу — от неё считается весь эффект, и менять
   её на 80-е сутки окна значит переписать почти готовый акт.
8. **Листалка в шапке** листает реестр в порядке по умолчанию: ссылка из
   реестра не несёт фильтров и сортировки. Когда понесёт — заменить запрос
   `getNeighbours`, разметка не изменится.

## Карточка: принятые решения

**Вкладка — сегмент адреса**, `/rec/[id]/[tab]`. Шапка и правая колонка живут в
`layout.tsx` и грузятся один раз; каждая вкладка — своя страница со своими
данными. Причина: карточку пересылают друг другу, и адрес вкладки ценен сам по
себе; тяжёлый расчёт эффекта изолируется естественно и не тормозит открытие
остальных вкладок. Разметку берём из `макет/card.html` + `card.css`, css
копируем файлом, как `registry.css`.

Вкладки карточки: Сводка, Реализация, Расчёт эффекта, Аналоги, Файлы,
История и обсуждение. Вкладка «Связи» из макета убрана по решению 100:
связанные сущности будут показаны в своём контексте. **Порядок изменён против макета:**
«Расчёт эффекта» стоит третьим, сразу за «Реализацией», — реализация открывает
окно, окно даёт эффект, две вкладки об одном и том же должны лежать рядом.
Состав и порядок — в `src/app/rec/[id]/tabs-def.ts`; вкладка без своей страницы
показывается приглушённой и не кликается, как несделанные пункты левой
навигации. `/rec/[id]` без сегмента уводит на первую вкладку с `ready: true`.

### Листалка в шапке (`pager.tsx`) и возврат в реестр (`back-link.tsx`)

Открыли карточку из отфильтрованного реестра — `?from=` (querystring отбора
без `page`/`size`) едет с ней по вкладкам, листалке и кнопке «К реестру».
Раньше «К реестру» вела на `/` без хвоста и теряла отбор — несогласованность
с вкладками и листалкой, а не сознательный сброс; поправлено (26.08.2026).

Позиция «N из M» в листалке при активном `from` считается не сетевым
запросом на каждый клик, а локально: список id всего отбора запрашивается
один раз и кэшируется в `sessionStorage` под ключом `from`, дальше prev/next/
pos — просто `indexOf` по массиву. Раньше на каждый переход слался свежий
запрос, и пока он не отвечал, было видно fallback (соседей по всему реестру
без фильтра) — мелькание вида «15 из 69» вместо «2 из 14». Кэш **не
ревалидируется** до следующего захода из реестра — решение пользователя:
скорость важнее свежести позиции в рамках одного просмотра.

### Вкладка «Расчёт эффекта»

Сверху вниз: итог в рублях с признаком «предварительный / окончательный»
(предварителен, пока окно открыто или есть незакрытый спор) → база с разбором
спора, если он есть → **прогресс окна** → график «факт против базы» → разложение
по статьям со ставками и версией модели → посуточная таблица (свёрнута, из неё
собирается Форма 5) → качество данных: сколько суток по замерам, сколько
протянуто.

**Прогресс окна — решение Эльдара, важное.** Шкала на все 90 суток: полная
шкала — прогнозный эффект (считается из ожидаемых приростов эксперта той же
формулой, см. `forecastTotal`), заполнение — накопленный факт. Прогноз **не пересчитывается** на прошедшие сутки: иначе
теряется то, с чем сравниваем. Вместо пересчёта — засечка «где факт должен быть
сейчас» = прогноз × (прошло суток / 90); по ней видно опережение или отставание.
После закрытия окна шкала показывает, попали ли в прогноз, — это материал для
оценки точности собственных прогнозов.

Если расчёт невозможен (нет ставок — таких около 30 % фонда, или нет замеров) —
объяснение, чего именно не хватает, а не пустой экран.

Расчёт кэшируется в `rec.effect_daily`: считать 90 суток заново при каждом
открытии расточительно, и он должен быть воспроизводим — в споре нужно
показать, из чего сложилась цифра, а не пересчитать по свежим данным. Правило
пересчёта: **окно открыто** — считаем заново при каждом открытии и переписываем
кэш (замеры догружаются задним числом); **окно закрыто** — отдаём кэш как есть
и на стенд не ходим вовсе. Досрочное закрытие обрезает окно по дате закрытия.

Акт верификации (Приложение № 5) на вкладку не выносим — будет отдельный экран.

### Листалка в шапке идёт по отбору реестра (22.08.2026)

Раньше «‹ N из M ›» в шапке карточки всегда листало весь реестр по умолчанию
(новые сверху) — фильтры и сортировка таблицы туда не доходили, это было
явно помечено как временное в комментарии `db/card.ts`. Теперь если карточку
открыли из отфильтрованной/отсортированной таблицы, листалка идёт по тому же
отбору.

Механика: `app/page.tsx` при активном фильтре кладёт querystring отбора в
ссылку строки — `/rec/{id}/summary?from=<урл-кодированный querystring>`.
`rec/[id]/tabs.tsx` тащит `from` дальше при переключении вкладок (иначе
терялось бы на первом клике). Сама листалка — `rec/[id]/pager.tsx`,
клиентский компонент: серверный `layout.tsx` searchParams не видит (Next их
layout-ам не отдаёт), поэтому `from` читается там через `useSearchParams()`,
а соседей по отбору считает серверный экшен `rec/[id]/pager-actions.ts` через
новый `getFilteredNeighbours` в `db/recommendations.ts` — то же
`buildConditions`/`SORT_EXPR`, что и у самого списка. Разбор querystring в
`ListFilter` вынесен в `parseListFilterFromSearchParams` (тот же файл) —
одна логика на кодирование (`page.tsx`) и декодирование (`pager-actions.ts`).

Без `?from=` (прямая ссылка, черновик, переход не из таблицы) поведение не
изменилось: `layout.tsx` по-прежнему сам считает соседей по умолчанию
(`getNeighbours` в `db/card.ts`) и передаёт как `fallback` — `Pager` рендерит
его без единого лишнего запроса.

## Тесты (Vitest) — заведены 22.08.2026

Тестовой инфраструктуры раньше не было вообще (только `tsc --noEmit` и снимки
глазами). Добавлен Vitest в двух конфигах — юнит и интеграционные тесты
разделены намеренно, разной ценой и разными зависимостями:

- `vitest.config.ts` + `npm test` — юнит-тесты чистых функций, без БД и без
  Next. `src/domain/*.test.ts` (все пять модулей домена) и
  `src/lib/access.test.ts` (`границаВидимости` и проверки ролей — тоже чистая
  функция, хоть и не в `domain/`). 75 тестов.
- `vitest.integration.config.ts` + `npm run test:integration` — бьют в
  реальный Postgres (нужен `npm run db:up`), моков БД нет намеренно (решение
  проекта — мок БД однажды разошёлся с продом). Покрыто: guard `vmapQuery`
  (отклоняет запись на чужом стенде), `registrationReferences`/
  `registrationAnalogs` (`db/registration.ts`), `listRecommendations`/
  `statusCounts`/`parseListFilterFromSearchParams` (`db/recommendations.ts`).
  18 тестов.

Функции, читающие `currentUser()` (реестр, счётчики), завязаны на
`next/headers` — вне запроса Next не работают. Для интеграционных тестов
сессия подменяется через `vi.mock('@/lib/session', ...)` на фиксированного
пользователя, а дальше весь путь идёт по-настоящему в БД — заменяется только
источник личности, не сама проверка границы видимости.

Тесты server actions с записью (создание рекомендации, статус-переходы,
решения) и снятого 90-суточного окна эффекта — не заведены: там нужны
транзакционные фикстуры (валидная скважина/направление/приоритет со всеми
FK) и стратегия отката после теста, чтобы не засорять демо-базу. Следующий
шаг, если тестирование продолжится.

## Грабли, на которые уже наступали

- **`app.css` грузится после `registry.css` и перебивает макет.** В нём не
  должно быть `.tbl`, `.panel`, `.btn`, `.field` — только собственные классы.
- **Утилиты Tailwind сталкиваются с классами макета по имени.** `block` — это
  и `display: block` у Tailwind, и блок карточки с отступом снизу в `card.css`.
  Компонент shadcn с `block` в классах молча получал 12 пикселов под каждой
  строкой. Лечится правилом `[data-slot].block { margin-bottom: 0 }` в
  `shadcn.css`; на очереди те же грабли с `card`, `panel`, `tag`, `field`.
- **React 19 сбрасывает форму с `action` после КАЖДОЙ отправки, в том числе
  неудачной.** Неуправляемые поля возвращаются к `defaultValue`: человек,
  споткнувшись о проверку, обнаруживал пустые поля и переключатель, сброшенный
  на первый пункт. Форма с валидацией на сервере обязана быть управляемой —
  состояние сброс переживает. Тот же дефект, скорее всего, есть в окнах спора
  о базе (`effect/baseline-forms.tsx`), их не трогали.
- **Часть утилит Tailwind в этой сборке просто не генерируется.** Правил нет в
  CSS, класс на элементе стоит, эффекта нет. Пойманы: `gap-x-*` / `gap-y-*`
  (обычный `gap-*` работает), `ring-*` с токеном в произвольном значении,
  `text-[var(--…)]` для цвета, `underline`. Проявляется молча — заливка встаёт,
  а белый текст на ней и рамка пропадают. Если утилита нужна и в проекте больше
  нигде не встречается, проверять вычисленный стиль, а не наличие класса; для
  цвета и рамок надёжнее инлайн со ссылкой на токен.
- **Токены надо проверять по `tokens.css`, а не по памяти.** `--accent-primary`,
  `--surface-sunken`, `--border-strong` не существуют; правильные —
  `--component-accent`, `--bg-tertiary`, `--border-component-default`.
  Несуществующий токен не ошибка: свойство просто не применяется, и клетка
  молча остаётся прозрачной.
- **Уникальность первичного ключа PostgreSQL проверяет ПОСТРОЧНО.** Массовый
  сдвиг дат (`SET date = date + N`) падает на собственных строках: запись за
  20-е приезжает на ещё не сдвинутую за 21-е. Порядком обновления управлять
  нельзя — `ORDER BY` в подзапросе его не задаёт. Лечится двумя шагами: уехать
  на заведомо пустой участок календаря целиком и вернуться со сдвигом
  (см. `rec.shift_demo`).
- **Свежепоставленный shadcn/animate-ui-компонент выглядит поломанным без
  preflight.** Сами компоненты никогда не сбрасывают браузерные умолчания
  (border, background, margin у `<h3>`/`<ul>`, `list-style`, appearance у
  `<button>`) — они писаны в расчёте, что это сделает preflight, а он у нас
  выключен (см. начало `shadcn.css`). Поймано на `AccordionTrigger` (рамка и
  серая заливка кнопки), `AccordionHeader` — это `<h3>` с margin ~16px сверху
  и снизу (казалось «скачком» при раскрытии), и `<ul>` в журнале истории
  (точки списка). Системная защита — `@layer base { [data-slot] { margin:0;
  padding:0; border:0; background:none; color:inherit; font:inherit;
  list-style:none; appearance:none } }` в `shadcn.css`: слой объявлен раньше
  `utilities`, поэтому проигрывает любому реальному классу компонента и виден
  только там, где сам компонент ничего не задал. Это НЕ отменяет точечные фиксы
  вроде `[data-slot].block` чуть выше в файле — та специфичная коллизия имён
  с макетом решается своим, уже существующим механизмом (правило вне слоёв,
  побеждает специально). Собственные `<ul>`/`<h1-6>` в коде приложения (не
  vendor-компоненты) `data-slot` не несут и по-прежнему нуждаются в ручном
  `list-none`/`m-0`.
- **react-day-picker, четыре места подряд.** Без `mode` календарь считается
  витриной и рисует дни текстом, а не кнопками — нажимать не на что. Объект
  `components`, собранный прямо в разметке (или в мемо с меняющимися
  зависимостями), даёт новый ТИП компонента на каждый рендер: React
  размонтирует клетку вместе с её состоянием, и окно дня закрывается само,
  стоит серверу прислать свежие данные. И обработчик дня зовёт
  `preventDefault`, поэтому `Popover.Trigger` от Radix свой клик пропускает —
  открытие приходится брать на себя. И `style` обязан стоять ПОСЛЕ `{...props}`:
  DayPicker передаёт кнопке собственный `style` (в том числе `undefined`) и
  затирает поставленный раньше — при этом `className` наоборот берётся свой, а
  чужой не применяется, иначе `bg-transparent` из `day_button` перебивает
  заливку. Все четыре — в `rec/[id]/daily/calendar.tsx`.
- **`db/vmap.ts` не импортируется из служебных скриптов:** он тянет
  `next/cache`, которого у голого node нет — падает `ERR_MODULE_NOT_FOUND` на
  ровном месте. Поэтому и запросы, и коды параметров (`PARAM`) живут в
  `db/vmap-sql.ts`, у которого зависимостей нет вовсе, а `vmap.ts` их
  реэкспортирует. Новую константу, нужную скриптам, класть туда же, а не в
  `vmap.ts`.
- **Установщик shadcn перезаписывает `Button.tsx`.** Он пишет `button.tsx`, а
  Windows не различает регистр — наш кастомный Button с вариантами ВМАП
  затирается стандартным. После каждого `shadcn add` смотреть `git diff` на
  `components/ui/`.
- **Необработанный отказ в промисе убивает процесс рендера целиком.** Ошибка в
  асинхронном подписчике SSE (его зовёт EventEmitter, где ловить некому) валила
  все страницы карточки сразу с `Jest worker encountered 2 child process
  exceptions`. Настоящее исключение Next прячет — искать по `--trace-uncaught`.
- **Два процесса Next на одном `.next`** дают то же `WorkerError` и
  `Cannot find module './NNN.js'`. Перед запуском сервера убеждаться, что
  старый умер: `Get-CimInstance Win32_Process` по `node.exe`.
- **Выпадающий список в окне действия рисовался ПОД окном.** `.combo__menu`
  стоял на слое 40, окно — на 50, наружу торчал только хвост меню. Теперь 60,
  как у календаря. Слой утилит Tailwind тут ни при чём: правило вне слоёв
  перебивает их независимо от значения.
- **`table-layout: fixed` требует явной ширины таблицы** (сумма колонок), иначе
  колонки растягиваются по содержимому.
- Ноль в `OperationMode` — полноценное значение (ЭЦН), а не пустота.
- Пласт из ВМАП (параметр 50, «БС10/1») и пласт в справочнике НДПИ
  («Восточно-Придорожный») — разные системы обозначений, сопоставлять их
  напрямую нельзя. Привязка берётся из модели Заказчика по номеру скважины.
- **Любой экспорт из модуля с `'use client'` приезжает на сервер клиентской
  ссылкой, а не значением.** Константа, взятая серверной страницей из
  клиентского файла, окажется прокси-объектом и молча испортит всё, куда её
  подставят (у нас — адрес редиректа, ошибка «Invalid character in header»).
  Поэтому данные вкладок лежат в `tabs-def.ts`, а компонент — в `tabs.tsx`.
- **Пересчёт кэша идёт параллельно сам с собой:** браузер предзагружает
  страницу и тут же её открывает, два рендера делают DELETE + INSERT
  одновременно, и в READ COMMITTED оба доходят до вставки. Лечится
  `pg_advisory_xact_lock` по `rec_id` плюс `ON CONFLICT DO UPDATE`.
- Ноль в плотности нефти или воды на стенде — незаполненное поле, а не лёгкая
  нефть. Такие записи есть, и ноль обнулил бы всю нефть по скважине.
- Headless Chrome не пишет скриншот по относительному пути и в системный temp:
  нужен абсолютный путь, кириллица в нём допустима.
- **`найтиСкроллер` (`ScrollOverlay.tsx`) отсеивал ещё-не-переполненный
  контейнер.** Условие `scrollHeight > clientHeight` проверялось один раз при
  монтировании: пустой чат умещался без прокрутки, скроллер навсегда
  запоминался как `null`, и автопрокрутка к новой реплике молчала даже когда
  лента потом выросла. Признак — структурный (`overflow-y: auto|scroll`), а не
  текущее переполнение.
- **Живой счётчик вкладки «История и обсуждение» не считал реплики, пока сама
  вкладка открыта.** `ЖивойСчётчикОбсуждения` (`tabs.tsx`) нарочно не слушал
  канал на вкладке `log` — рассуждение было «чат и так покажет», но полоса
  вкладок живёт в layout и не перерисовывается при отправке реплики. Число
  рядом с названием вкладки застывало до перехода на другую вкладку и обратно.
  Канал теперь слушается всегда, точка «есть новое» — по-прежнему только вне
  `log`.
- **`.context` (правая колонка карточки) обрезал тень `.card` по горизонтали.**
  `overflow-y: auto` по спецификации переводит `overflow-x` тоже в `auto`,
  а тень (единственная рамка `.card` — border у него нет) выходит за границу
  бокса на несколько пикселов и обрезается вплотную. Лечится небольшим
  `padding` на `.context` — тени внутри есть куда деваться, ширина у карточек
  не фиксированная и подстраивается сама.
- **С российских IP (в том числе Yandex Cloud VM) не отвечают:
  `download.docker.com`, `registry-1.docker.io` (Docker Hub), `github.com:22`,
  `registry.npmjs.org`.** Все таймаутят молча, без внятной ошибки блокировки.
  Обходы, зашитые в `deploy/setup-vm-base.sh`: Docker ставится пакетом
  `docker.io` из репозитория Ubuntu, а не с `download.docker.com`; для образов
  Docker настроено `registry-mirrors: ["https://mirror.gcr.io"]` в
  `/etc/docker/daemon.json`; GitHub — по SSH через `ssh.github.com:443` вместо
  `:22` (тоже блокируют); npm — `registry set https://registry.npmmirror.com`.
- **`sudo -u user cmd >> file` пишет файл от имени ТОГО, кто вызвал sudo (root),
  а не от `user`.** Редирект `>>` открывает файловый дескриптор внешняя
  оболочка, до того как `sudo` меняет пользователя. Ловушка: `ssh-keyscan ... >>
  known_hosts` для сервисного пользователя создавал файл от root, и та же
  учётка потом не могла обновить `known_hosts` через git —
  `Operation not permitted`. Лечится редиректом ВНУТРИ `sudo -u user bash -c
  '... >> file'` или явным `chown` после.
- **`git clone` не терпит непустую целевую директорию** — даже если там только
  скрытые файлы. Из-за этого нельзя делать домашний каталог сервисного
  пользователя (куда кладётся SSH-ключ) тем же путём, куда клонируется
  репозиторий: `useradd -d` и цель `git clone` должны быть разными путями
  (см. `deploy/setup-vm-base.sh` — `/home/recmodule` и `/opt/recmodule`).
- **`db:migrate -- --baseline` требует, чтобы схема `rec` УЖЕ существовала** —
  он только помечает миграции применёнными, не выполняет их. На чистой базе
  (первый деплой) падает `schema "rec" does not exist`. На пустой базе нужен
  обычный `npm run db:migrate` без `--baseline`.
- **VPN с `AllowedIPs = 0.0.0.0/0` и включённым kill-switch заворачивает в
  туннель вообще весь трафик**, включая к серверам в том же Yandex Cloud —
  никакой маршрут Windows (`route add ... metric 1`) это не обходит: WireGuard
  и kill-switch работают ниже таблицы маршрутизации ОС. Единственный рабочий
  способ исключить один IP — переписать `AllowedIPs` на полный список подсетей
  IPv4 МИНУС этот адрес (32 диапазона, `ipaddress.ip_network('0.0.0.0/0')
  .address_exclude(...)` в Python). Привязано к конкретному IP: сменится IP
  VM — пересчитывать заново.

## Документы проекта (корень)

`Дизайн модуля — принятые решения.md` — источник истины, 95 решений, часть
отменена более поздними. `Расхождения с договором.md`, `Вопросы к обсуждению.md`,
`ВМАП — визуальный язык.md`, `tokens.css`.
