/** @type {import('next').NextConfig} */
const nextConfig = {
  // Кэш серверного рендера выключен (все страницы force-dynamic): модуль
  // внутренний, статусы меняются постоянно, и двое пользователей смотрят один
  // реестр — отдать им сохранённую копию значит показать «На рассмотрении»
  // там, где уже «Согласовано».
  //
  // А вот КЛИЕНТСКИЙ кэш маршрутов (staleTimes) держать на нуле было
  // перебором: он касается только переходов внутри одной вкладки браузера, и
  // ноль означал поход на сервер при каждом «назад» и при каждом возврате на
  // вкладку карточки, где человек был секунду назад. Любое действие вызывает
  // revalidatePath и сбрасывает этот кэш само.
  //
  // Три минуты, а не полминуты: столько живёт предзагрузка вкладок карточки
  // (`ПрогревВкладок` в rec/[id]/tabs.tsx), и с коротким сроком она теряла
  // смысл — после минуты чтения сводки переход на соседнюю вкладку снова шёл
  // на сервер со скелетоном. Плата: страница, открытая повторно в пределах
  // трёх минут, показывается такой, какой её прочитали, — чужое решение или
  // смена статуса на ней не видны. Лента обсуждения от этого не зависит, она
  // живёт на SSE.
  experimental: { staleTimes: { dynamic: 180, static: 0 } },

  // `pg` не собирать бандлером, а требовать из node_modules как есть. У него
  // есть необязательная нативная ветка (`pg-native`), которой у нас нет:
  // сборщик пытается её разрешить, ругается на каждый запрос и ломает граф
  // модулей, когда pg попадает ещё и в бандл инструментации.
  serverExternalPackages: ['pg'],
};
export default nextConfig;
