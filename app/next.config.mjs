/** @type {import('next').NextConfig} */
const nextConfig = {
  // Модуль внутренний, за авторизацией: кэш страниц здесь вреден. Статусы
  // рекомендаций меняются постоянно, и двое пользователей смотрят один реестр —
  // отдать им сохранённую копию значит показать «На рассмотрении» там, где уже
  // «Согласовано». Кэширование включается точечно там, где оно доказанно нужно.
  experimental: { staleTimes: { dynamic: 0, static: 0 } },
};
export default nextConfig;
