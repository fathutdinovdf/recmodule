/* Имена кук входа — отдельным файлом без единого импорта.
 *
 * Их читает middleware, а он исполняется в edge-окружении: любой импорт,
 * который тянет за собой `pg`, ломает сборку. Держать константы рядом с
 * `currentUser` (а он ходит в базу) значит однажды получить именно это.
 */

export const КУКА_СЕССИИ = 'rec-session';

/** Кука подмены входа: только разработка, см. `session.ts`. */
export const КУКА_ПОЛЬЗОВАТЕЛЯ = 'demo-user';
