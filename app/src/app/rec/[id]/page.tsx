/* Карточка без сегмента вкладки — ссылка из реестра и из уведомлений.
   Уводит на первую вкладку, у которой есть своя страница: пока сделан один
   «Расчёт эффекта», и адрес /rec/12 должен вести к нему, а не в пустоту.
   Когда появится «Сводка», она встанет первой в списке вкладок и станет
   умолчанием сама — здесь править ничего не придётся. */

import { redirect } from 'next/navigation';
import { ВКЛАДКА_ПО_УМОЛЧАНИЮ } from './tabs-def';

export default async function Page({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  redirect(`/rec/${id}/${ВКЛАДКА_ПО_УМОЛЧАНИЮ}`);
}
