/* Имя cookie видимости колонок — общее для серверной страницы (читает) и
 * клиентского поповера (пишет). Отдельным файлом без 'use client': экспорт
 * константы из клиентского модуля приезжает на сервер клиентской ссылкой,
 * а не значением (см. tabs-def.ts/tabs.tsx и «грабли» в CLAUDE.md), и
 * cookies().get(ссылка) молча возвращал undefined. */
export const ВИДИМЫЕ_КОЛОНКИ_COOKIE = 'reg_cols';
