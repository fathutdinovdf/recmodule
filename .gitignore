# Скомпилированный Python из fetch_fields*.py
__pycache__/
*.pyc

# Собранные архивы для передачи — не источник истины, пересобираются из макет/
*.zip

# Архивы для передачи — пересобираются из макет/
*.rar

# Скриншоты экранов и временные пробники — пересобираются рендером из макет/
скриншоты/
макет/_probe.html
макет/_verify.html

# Выгрузка замеров — 18 МБ, пересоздаётся скриптом выгрузка_вмап_замеры.py.
# В git не нужна: это снимок чужой базы, а не исходник проекта.
вмап-замеры.json
